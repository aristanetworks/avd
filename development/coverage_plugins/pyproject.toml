[project]
name = "coverage-plugins"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["coverage[toml]>=7.0", "jinja2>=3.0"]

[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"
