# Copyright (c) 2023-2026 Arista Networks, Inc.
# Use of this source code is governed by the Apache License 2.0
# that can be found in the LICENSE file.
# yaml-language-server: $schema=../../../_schema/avd_meta_schema.json
# Line above is used by RedHat's YAML Schema vscode extension
# Use Ctrl + Space to get suggestions for every field. Autocomplete will pop up after typing 2 letters.
type: dict
keys:
  eos_designs_digital_twin_mode:
    documentation_options:
      table: role-settings
    type: bool
    description: |-
      PREVIEW: This option is marked as "preview", meaning the data models or generated configuration can change at any time.
      Enable generation of the Digital Twin version of the fabric (Digital Twin topology, adjusted configuration, etc.).
      By default, Digital Twin artifacts (such as the topology file, adjusted structured and EOS configuration, device and fabric documentation) will replace original fabric artifacts.
      To keep Digital Twin artifacts separate, adjust `eos_designs_output_dir_name`, `eos_designs_documentation_dir_name`,
      `eos_cli_config_gen_output_dir_name`, and `eos_cli_config_gen_documentation_dir_name` to point to dedicated output locations.
    default: false
  avd_digital_twin_mode:
    documentation_options:
      table: legacy-role-settings
    type: bool
    description: Legacy alias for `eos_designs_digital_twin_mode`.
