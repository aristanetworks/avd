# Copyright (c) 2026 Arista Networks, Inc.
# Use of this source code is governed by the Apache License 2.0
# that can be found in the LICENSE file.
# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: google/protobuf/descriptor.proto, google/protobuf/duration.proto, google/protobuf/timestamp.proto, google/protobuf/wrappers.proto
# plugin: python-aristaproto
# This file has been @generated

__all__ = (
    "BoolValue",
    "BytesValue",
    "DescriptorProto",
    "DescriptorProtoExtensionRange",
    "DescriptorProtoReservedRange",
    "DoubleValue",
    "Duration",
    "Edition",
    "EnumDescriptorProto",
    "EnumDescriptorProtoEnumReservedRange",
    "EnumOptions",
    "EnumValueDescriptorProto",
    "EnumValueOptions",
    "ExtensionRangeOptions",
    "ExtensionRangeOptionsDeclaration",
    "ExtensionRangeOptionsVerificationState",
    "FeatureSet",
    "FeatureSetDefaults",
    "FeatureSetDefaultsFeatureSetEditionDefault",
    "FeatureSetEnforceNamingStyle",
    "FeatureSetEnumType",
    "FeatureSetFieldPresence",
    "FeatureSetJsonFormat",
    "FeatureSetMessageEncoding",
    "FeatureSetRepeatedFieldEncoding",
    "FeatureSetUtf8Validation",
    "FeatureSetVisibilityFeature",
    "FeatureSetVisibilityFeatureDefaultSymbolVisibility",
    "FieldDescriptorProto",
    "FieldDescriptorProtoLabel",
    "FieldDescriptorProtoType",
    "FieldOptions",
    "FieldOptionsCType",
    "FieldOptionsEditionDefault",
    "FieldOptionsFeatureSupport",
    "FieldOptionsJsType",
    "FieldOptionsOptionRetention",
    "FieldOptionsOptionTargetType",
    "FileDescriptorProto",
    "FileDescriptorSet",
    "FileOptions",
    "FileOptionsOptimizeMode",
    "FloatValue",
    "GeneratedCodeInfo",
    "GeneratedCodeInfoAnnotation",
    "GeneratedCodeInfoAnnotationSemantic",
    "Int32Value",
    "Int64Value",
    "MessageOptions",
    "MethodDescriptorProto",
    "MethodOptions",
    "MethodOptionsIdempotencyLevel",
    "OneofDescriptorProto",
    "OneofOptions",
    "ServiceDescriptorProto",
    "ServiceOptions",
    "SourceCodeInfo",
    "SourceCodeInfoLocation",
    "StringValue",
    "SymbolVisibility",
    "Timestamp",
    "UInt32Value",
    "UInt64Value",
    "UninterpretedOption",
    "UninterpretedOptionNamePart",
)

import datetime
import re
import typing
import warnings
from dataclasses import dataclass

import aristaproto
from aristaproto.nano_datetime import NanoDatetime
from typing_extensions import Self

from ...message_pool import default_message_pool

_COMPILER_VERSION = "2.0.0.dev1"
aristaproto.check_compiler_version(_COMPILER_VERSION)


class Edition(aristaproto.Enum):
    """
    The full set of known editions.
    """

    UNKNOWN = 0
    """
    A placeholder for an unknown edition value.
    """

    LEGACY = 900
    """
    A placeholder edition for specifying default behaviors *before* a feature
    was first introduced.  This is effectively an "infinite past".
    """

    PROTO2 = 998
    """
    Legacy syntax "editions".  These pre-date editions, but behave much like
    distinct editions.  These can't be used to specify the edition of proto
    files, but feature definitions must supply proto2/proto3 defaults for
    backwards compatibility.
    """

    PROTO3 = 999

    _2023 = 1000
    """
    Editions that have been released.  The specific values are arbitrary and
    should not be depended on, but they will always be time-ordered for easy
    comparison.
    """

    _2024 = 1001

    UNSTABLE = 9999
    """
    A placeholder edition for developing and testing unscheduled features.
    """

    _1_TEST_ONLY = 1
    """
    Placeholder editions for testing feature resolution.  These should not be
    used or relied on outside of tests.
    """

    _2_TEST_ONLY = 2

    _99997_TEST_ONLY = 99997

    _99998_TEST_ONLY = 99998

    _99999_TEST_ONLY = 99999

    MAX = 2147483647
    """
    Placeholder for specifying unbounded edition support.  This should only
    ever be used by plugins that can expect to never require any changes to
    support a new edition.
    """

    @classmethod
    def aristaproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "EDITION_UNKNOWN",
            900: "EDITION_LEGACY",
            998: "EDITION_PROTO2",
            999: "EDITION_PROTO3",
            1000: "EDITION_2023",
            1001: "EDITION_2024",
            9999: "EDITION_UNSTABLE",
            1: "EDITION_1_TEST_ONLY",
            2: "EDITION_2_TEST_ONLY",
            99997: "EDITION_99997_TEST_ONLY",
            99998: "EDITION_99998_TEST_ONLY",
            99999: "EDITION_99999_TEST_ONLY",
            2147483647: "EDITION_MAX",
        }

    @classmethod
    def aristaproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "EDITION_UNKNOWN": 0,
            "EDITION_LEGACY": 900,
            "EDITION_PROTO2": 998,
            "EDITION_PROTO3": 999,
            "EDITION_2023": 1000,
            "EDITION_2024": 1001,
            "EDITION_UNSTABLE": 9999,
            "EDITION_1_TEST_ONLY": 1,
            "EDITION_2_TEST_ONLY": 2,
            "EDITION_99997_TEST_ONLY": 99997,
            "EDITION_99998_TEST_ONLY": 99998,
            "EDITION_99999_TEST_ONLY": 99999,
            "EDITION_MAX": 2147483647,
        }


class ExtensionRangeOptionsVerificationState(aristaproto.Enum):
    """
    The verification state of the extension range.
    """

    DECLARATION = 0
    """
    All the extensions of the range must be declared.
    """

    UNVERIFIED = 1


class FeatureSetEnforceNamingStyle(aristaproto.Enum):
    ENFORCE_NAMING_STYLE_UNKNOWN = 0

    STYLE2024 = 1

    STYLE_LEGACY = 2


class FeatureSetEnumType(aristaproto.Enum):
    ENUM_TYPE_UNKNOWN = 0

    OPEN = 1

    CLOSED = 2


class FeatureSetFieldPresence(aristaproto.Enum):
    FIELD_PRESENCE_UNKNOWN = 0

    EXPLICIT = 1

    IMPLICIT = 2

    LEGACY_REQUIRED = 3


class FeatureSetJsonFormat(aristaproto.Enum):
    JSON_FORMAT_UNKNOWN = 0

    ALLOW = 1

    LEGACY_BEST_EFFORT = 2


class FeatureSetMessageEncoding(aristaproto.Enum):
    MESSAGE_ENCODING_UNKNOWN = 0

    LENGTH_PREFIXED = 1

    DELIMITED = 2


class FeatureSetRepeatedFieldEncoding(aristaproto.Enum):
    REPEATED_FIELD_ENCODING_UNKNOWN = 0

    PACKED = 1

    EXPANDED = 2


class FeatureSetUtf8Validation(aristaproto.Enum):
    UTF8_VALIDATION_UNKNOWN = 0

    VERIFY = 2

    NONE = 3


class FeatureSetVisibilityFeatureDefaultSymbolVisibility(aristaproto.Enum):
    DEFAULT_SYMBOL_VISIBILITY_UNKNOWN = 0

    EXPORT_ALL = 1
    """
    Default pre-EDITION_2024, all UNSET visibility are export.
    """

    EXPORT_TOP_LEVEL = 2
    """
    All top-level symbols default to export, nested default to local.
    """

    LOCAL_ALL = 3
    """
    All symbols default to local.
    """

    STRICT = 4
    """
    All symbols local by default. Nested types cannot be exported.
    With special case caveat for message { enum {} reserved 1 to max; }
    This is the recommended setting for new protos.
    """


class FieldDescriptorProtoLabel(aristaproto.Enum):
    OPTIONAL = 1
    """
    0 is reserved for errors
    """

    REPEATED = 3

    REQUIRED = 2
    """
    The required label is only allowed in google.protobuf.  In proto3 and Editions
    it's explicitly prohibited.  In Editions, the `field_presence` feature
    can be used to get this behavior.
    """

    @classmethod
    def aristaproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            1: "LABEL_OPTIONAL",
            3: "LABEL_REPEATED",
            2: "LABEL_REQUIRED",
        }

    @classmethod
    def aristaproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "LABEL_OPTIONAL": 1,
            "LABEL_REPEATED": 3,
            "LABEL_REQUIRED": 2,
        }


class FieldDescriptorProtoType(aristaproto.Enum):
    DOUBLE = 1
    """
    0 is reserved for errors.
    Order is weird for historical reasons.
    """

    FLOAT = 2

    INT64 = 3
    """
    Not ZigZag encoded.  Negative numbers take 10 bytes.  Use TYPE_SINT64 if
    negative values are likely.
    """

    UINT64 = 4

    INT32 = 5
    """
    Not ZigZag encoded.  Negative numbers take 10 bytes.  Use TYPE_SINT32 if
    negative values are likely.
    """

    FIXED64 = 6

    FIXED32 = 7

    BOOL = 8

    STRING = 9

    GROUP = 10
    """
    Tag-delimited aggregate.
    Group type is deprecated and not supported after google.protobuf. However, Proto3
    implementations should still be able to parse the group wire format and
    treat group fields as unknown fields.  In Editions, the group wire format
    can be enabled via the `message_encoding` feature.
    """

    MESSAGE = 11
    """
    Length-delimited aggregate.
    """

    BYTES = 12
    """
    New in version 2.
    """

    UINT32 = 13

    ENUM = 14

    SFIXED32 = 15

    SFIXED64 = 16

    SINT32 = 17
    """
    Uses ZigZag encoding.
    """

    SINT64 = 18
    """
    Uses ZigZag encoding.
    """

    @classmethod
    def aristaproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            1: "TYPE_DOUBLE",
            2: "TYPE_FLOAT",
            3: "TYPE_INT64",
            4: "TYPE_UINT64",
            5: "TYPE_INT32",
            6: "TYPE_FIXED64",
            7: "TYPE_FIXED32",
            8: "TYPE_BOOL",
            9: "TYPE_STRING",
            10: "TYPE_GROUP",
            11: "TYPE_MESSAGE",
            12: "TYPE_BYTES",
            13: "TYPE_UINT32",
            14: "TYPE_ENUM",
            15: "TYPE_SFIXED32",
            16: "TYPE_SFIXED64",
            17: "TYPE_SINT32",
            18: "TYPE_SINT64",
        }

    @classmethod
    def aristaproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "TYPE_DOUBLE": 1,
            "TYPE_FLOAT": 2,
            "TYPE_INT64": 3,
            "TYPE_UINT64": 4,
            "TYPE_INT32": 5,
            "TYPE_FIXED64": 6,
            "TYPE_FIXED32": 7,
            "TYPE_BOOL": 8,
            "TYPE_STRING": 9,
            "TYPE_GROUP": 10,
            "TYPE_MESSAGE": 11,
            "TYPE_BYTES": 12,
            "TYPE_UINT32": 13,
            "TYPE_ENUM": 14,
            "TYPE_SFIXED32": 15,
            "TYPE_SFIXED64": 16,
            "TYPE_SINT32": 17,
            "TYPE_SINT64": 18,
        }


class FieldOptionsCType(aristaproto.Enum):
    STRING = 0
    """
    Default mode.
    """

    CORD = 1
    """
    The option [ctype=CORD] may be applied to a non-repeated field of type
    "bytes". It indicates that in C++, the data should be stored in a Cord
    instead of a string.  For very large strings, this may reduce memory
    fragmentation. It may also allow better performance when parsing from a
    Cord, or when parsing with aliasing enabled, as the parsed Cord may then
    alias the original buffer.
    """

    STRING_PIECE = 2


class FieldOptionsJsType(aristaproto.Enum):
    JS_NORMAL = 0
    """
    Use the default type.
    """

    JS_STRING = 1
    """
    Use JavaScript strings.
    """

    JS_NUMBER = 2
    """
    Use JavaScript numbers.
    """


class FieldOptionsOptionRetention(aristaproto.Enum):
    """
    If set to RETENTION_SOURCE, the option will be omitted from the binary.
    """

    RETENTION_UNKNOWN = 0

    RETENTION_RUNTIME = 1

    RETENTION_SOURCE = 2


class FieldOptionsOptionTargetType(aristaproto.Enum):
    """
    This indicates the types of entities that the field may apply to when used
    as an option. If it is unset, then the field may be freely used as an
    option on any kind of entity.
    """

    TARGET_TYPE_UNKNOWN = 0

    TARGET_TYPE_FILE = 1

    TARGET_TYPE_EXTENSION_RANGE = 2

    TARGET_TYPE_MESSAGE = 3

    TARGET_TYPE_FIELD = 4

    TARGET_TYPE_ONEOF = 5

    TARGET_TYPE_ENUM = 6

    TARGET_TYPE_ENUM_ENTRY = 7

    TARGET_TYPE_SERVICE = 8

    TARGET_TYPE_METHOD = 9


class FileOptionsOptimizeMode(aristaproto.Enum):
    """
    Generated classes can be optimized for speed or code size.
    """

    SPEED = 1
    """
    Generate complete code for parsing, serialization,
    """

    CODE_SIZE = 2
    """
    etc.

    Use ReflectionOps to implement these methods.
    """

    LITE_RUNTIME = 3
    """
    Generate code using MessageLite and the lite runtime.
    """


class GeneratedCodeInfoAnnotationSemantic(aristaproto.Enum):
    """
    Represents the identified object's effect on the element in the original
    .proto file.
    """

    NONE = 0
    """
    There is no effect or the effect is indescribable.
    """

    SET = 1
    """
    The element is set or otherwise mutated.
    """

    ALIAS = 2
    """
    An alias to the element is returned.
    """


class MethodOptionsIdempotencyLevel(aristaproto.Enum):
    """
    Is this method side-effect-free (or safe in HTTP parlance), or idempotent,
    or neither? HTTP based RPC implementation may choose GET verb for safe
    methods, and PUT verb for idempotent methods instead of the default POST.
    """

    IDEMPOTENCY_UNKNOWN = 0

    NO_SIDE_EFFECTS = 1
    """
    implies idempotent
    """

    IDEMPOTENT = 2
    """
    idempotent, but may have side effects
    """


class SymbolVisibility(aristaproto.Enum):
    """
    Describes the 'visibility' of a symbol with respect to the proto import
    system. Symbols can only be imported when the visibility rules do not prevent
    it (ex: local symbols cannot be imported).  Visibility modifiers can only set
    on `message` and `enum` as they are the only types available to be referenced
    from other files.
    """

    VISIBILITY_UNSET = 0

    VISIBILITY_LOCAL = 1

    VISIBILITY_EXPORT = 2


@dataclass(eq=False, repr=False)
class BoolValue(aristaproto.Message):
    """
    Wrapper message for `bool`.

    The JSON representation for `BoolValue` is JSON `true` and `false`.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "bool" = aristaproto.field(1, aristaproto.TYPE_BOOL)
    """
    The bool value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, bool):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: bool) -> "BoolValue":
        return BoolValue(value=wrapped)

    def to_wrapped(self) -> bool:
        return self.value


default_message_pool.register_message("google.protobuf", "BoolValue", BoolValue)


@dataclass(eq=False, repr=False)
class BytesValue(aristaproto.Message):
    """
    Wrapper message for `bytes`.

    The JSON representation for `BytesValue` is JSON string.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "bytes" = aristaproto.field(1, aristaproto.TYPE_BYTES)
    """
    The bytes value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, bytes):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: bytes) -> "BytesValue":
        return BytesValue(value=wrapped)

    def to_wrapped(self) -> bytes:
        return self.value


default_message_pool.register_message("google.protobuf", "BytesValue", BytesValue)


@dataclass(eq=False, repr=False)
class DescriptorProto(aristaproto.Message):
    """
    Describes a message type.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    field: "list[FieldDescriptorProto]" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, repeated=True)

    extension: "list[FieldDescriptorProto]" = aristaproto.field(6, aristaproto.TYPE_MESSAGE, repeated=True)

    nested_type: "list[DescriptorProto]" = aristaproto.field(3, aristaproto.TYPE_MESSAGE, repeated=True)

    enum_type: "list[EnumDescriptorProto]" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, repeated=True)

    extension_range: "list[DescriptorProtoExtensionRange]" = aristaproto.field(5, aristaproto.TYPE_MESSAGE, repeated=True)

    oneof_decl: "list[OneofDescriptorProto]" = aristaproto.field(8, aristaproto.TYPE_MESSAGE, repeated=True)

    options: "MessageOptions | None" = aristaproto.field(7, aristaproto.TYPE_MESSAGE, optional=True)

    reserved_range: "list[DescriptorProtoReservedRange]" = aristaproto.field(9, aristaproto.TYPE_MESSAGE, repeated=True)

    reserved_name: "list[str]" = aristaproto.field(10, aristaproto.TYPE_STRING, repeated=True)
    """
    Reserved field names, which may not be used by fields in the same message.
    A given name may only be reserved once.
    """

    visibility: "SymbolVisibility" = aristaproto.field(11, aristaproto.TYPE_ENUM, default_factory=lambda: SymbolVisibility(0))
    """
    Support for `export` and `local` keywords on enums.
    """


default_message_pool.register_message("google.protobuf", "DescriptorProto", DescriptorProto)


@dataclass(eq=False, repr=False)
class DescriptorProtoExtensionRange(aristaproto.Message):
    start: "int" = aristaproto.field(1, aristaproto.TYPE_INT32)
    """
    Inclusive.
    """

    end: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)
    """
    Exclusive.
    """

    options: "ExtensionRangeOptions | None" = aristaproto.field(3, aristaproto.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("google.protobuf", "DescriptorProto.ExtensionRange", DescriptorProtoExtensionRange)


@dataclass(eq=False, repr=False)
class DescriptorProtoReservedRange(aristaproto.Message):
    """
    Range of reserved tag numbers. Reserved tag numbers may not be used by
    fields or extension ranges in the same message. Reserved ranges may
    not overlap.
    """

    start: "int" = aristaproto.field(1, aristaproto.TYPE_INT32)
    """
    Inclusive.
    """

    end: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)
    """
    Exclusive.
    """


default_message_pool.register_message("google.protobuf", "DescriptorProto.ReservedRange", DescriptorProtoReservedRange)


@dataclass(eq=False, repr=False)
class DoubleValue(aristaproto.Message):
    """
    Wrapper message for `double`.

    The JSON representation for `DoubleValue` is JSON number.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "float" = aristaproto.field(1, aristaproto.TYPE_DOUBLE)
    """
    The double value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, float):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: float) -> "DoubleValue":
        return DoubleValue(value=wrapped)

    def to_wrapped(self) -> float:
        return self.value


default_message_pool.register_message("google.protobuf", "DoubleValue", DoubleValue)


@dataclass(eq=False, repr=False)
class Duration(aristaproto.Message):
    """
    A Duration represents a signed, fixed-length span of time represented
    as a count of seconds and fractions of seconds at nanosecond
    resolution. It is independent of any calendar and concepts like "day"
    or "month". It is related to Timestamp in that the difference between
    two Timestamp values is a Duration and it can be added or subtracted
    from a Timestamp. Range is approximately +-10,000 years.

    # Examples

    Example 1: Compute Duration from two Timestamps in pseudo code.

        Timestamp start = ...;
        Timestamp end = ...;
        Duration duration = ...;

        duration.seconds = end.seconds - start.seconds;
        duration.nanos = end.nanos - start.nanos;

        if (duration.seconds < 0 && duration.nanos > 0) {
          duration.seconds += 1;
          duration.nanos -= 1000000000;
        } else if (duration.seconds > 0 && duration.nanos < 0) {
          duration.seconds -= 1;
          duration.nanos += 1000000000;
        }

    Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.

        Timestamp start = ...;
        Duration duration = ...;
        Timestamp end = ...;

        end.seconds = start.seconds + duration.seconds;
        end.nanos = start.nanos + duration.nanos;

        if (end.nanos < 0) {
          end.seconds -= 1;
          end.nanos += 1000000000;
        } else if (end.nanos >= 1000000000) {
          end.seconds += 1;
          end.nanos -= 1000000000;
        }

    Example 3: Compute Duration from datetime.timedelta in Python.

        td = datetime.timedelta(days=3, minutes=10)
        duration = Duration()
        duration.FromTimedelta(td)

    # JSON Mapping

    In JSON format, the Duration type is encoded as a string rather than an
    object, where the string ends in the suffix "s" (indicating seconds) and
    is preceded by the number of seconds, with nanoseconds expressed as
    fractional seconds. For example, 3 seconds with 0 nanoseconds should be
    encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
    be expressed in JSON format as "3.000000001s", and 3 seconds and 1
    microsecond should be expressed in JSON format as "3.000001s".
    """

    seconds: "int" = aristaproto.field(1, aristaproto.TYPE_INT64)
    """
    Signed seconds of the span of time. Must be from -315,576,000,000
    to +315,576,000,000 inclusive. Note: these bounds are computed from:
    60 sec/min * 60 min/hr * 24 hr/day * 365.25 days/year * 10000 years
    """

    nanos: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)
    """
    Signed fractions of a second at nanosecond resolution of the span
    of time. Durations less than one second are represented with a 0
    `seconds` field and a positive or negative `nanos` field. For durations
    of one second or more, a non-zero value for the `nanos` field must be
    of the same sign as the `seconds` field. Must be from -999,999,999
    to +999,999,999 inclusive.
    """

    @classmethod
    def from_timedelta(cls, delta: datetime.timedelta, *, _1_microsecond: datetime.timedelta = datetime.timedelta(microseconds=1)) -> "Duration":
        total_ms = delta // _1_microsecond
        seconds = int(total_ms / 1e6)
        nanos = int((total_ms % 1e6) * 1e3)
        return cls(seconds, nanos)

    def to_timedelta(self) -> datetime.timedelta:
        return datetime.timedelta(seconds=self.seconds, microseconds=self.nanos / 1e3)

    @staticmethod
    def delta_to_json(delta: datetime.timedelta) -> str:
        parts = str(delta.total_seconds()).split(".")
        if len(parts) > 1:
            while len(parts[1]) not in (3, 6, 9):
                parts[1] = f"{parts[1]}0"
        return f"{'.'.join(parts)}s"

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, str):
            if not re.match(r"^\d+(\.\d+)?s$", value):
                raise ValueError(f"Invalid duration string: {value}")

            seconds = float(value[:-1])
            return cls(seconds=int(seconds), nanos=int((seconds - int(seconds)) * 1e9))

        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        # If the output format is PYTHON, we should have kept the wrapped type without building the real class
        assert output_format == aristaproto.OutputFormat.PROTO_JSON

        assert 0 <= self.nanos < 1e9

        if self.nanos == 0:
            return f"{self.seconds}s"

        nanos = f"{self.nanos:09d}".rstrip("0")
        if len(nanos) < 3:
            nanos += "0" * (3 - len(nanos))

        return f"{self.seconds}.{nanos}s"

    @staticmethod
    def from_wrapped(wrapped: datetime.timedelta) -> "Duration":
        return Duration.from_timedelta(wrapped)

    def to_wrapped(self) -> datetime.timedelta:
        return self.to_timedelta()


default_message_pool.register_message("google.protobuf", "Duration", Duration)


@dataclass(eq=False, repr=False)
class EnumDescriptorProto(aristaproto.Message):
    """
    Describes an enum type.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    value: "list[EnumValueDescriptorProto]" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, repeated=True)

    options: "EnumOptions | None" = aristaproto.field(3, aristaproto.TYPE_MESSAGE, optional=True)

    reserved_range: "list[EnumDescriptorProtoEnumReservedRange]" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    Range of reserved numeric values. Reserved numeric values may not be used
    by enum values in the same enum declaration. Reserved ranges may not
    overlap.
    """

    reserved_name: "list[str]" = aristaproto.field(5, aristaproto.TYPE_STRING, repeated=True)
    """
    Reserved enum value names, which may not be reused. A given name may only
    be reserved once.
    """

    visibility: "SymbolVisibility" = aristaproto.field(6, aristaproto.TYPE_ENUM, default_factory=lambda: SymbolVisibility(0))
    """
    Support for `export` and `local` keywords on enums.
    """


default_message_pool.register_message("google.protobuf", "EnumDescriptorProto", EnumDescriptorProto)


@dataclass(eq=False, repr=False)
class EnumDescriptorProtoEnumReservedRange(aristaproto.Message):
    """
    Range of reserved numeric values. Reserved values may not be used by
    entries in the same enum. Reserved ranges may not overlap.

    Note that this is distinct from DescriptorProto.ReservedRange in that it
    is inclusive such that it can appropriately represent the entire int32
    domain.
    """

    start: "int" = aristaproto.field(1, aristaproto.TYPE_INT32)
    """
    Inclusive.
    """

    end: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)
    """
    Inclusive.
    """


default_message_pool.register_message("google.protobuf", "EnumDescriptorProto.EnumReservedRange", EnumDescriptorProtoEnumReservedRange)


@dataclass(eq=False, repr=False)
class EnumOptions(aristaproto.Message):
    allow_alias: "bool" = aristaproto.field(2, aristaproto.TYPE_BOOL)
    """
    Set this option to true to allow mapping different tag names to the same
    value.
    """

    deprecated: "bool" = aristaproto.field(3, aristaproto.TYPE_BOOL)
    """
    Is this enum deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for the enum, or it will be completely ignored; in the very least, this
    is a formalization for deprecating enums.
    """

    deprecated_legacy_json_field_conflicts: "bool" = aristaproto.field(6, aristaproto.TYPE_BOOL)
    """
    Enable the legacy handling of JSON field name conflicts.  This lowercases
    and strips underscored from the fields before comparison in proto3 only.
    The new behavior takes `json_name` into account and applies to proto2 as
    well.
    TODO Remove this legacy behavior once downstream teams have
    had time to migrate.
    """

    features: "FeatureSet | None" = aristaproto.field(7, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """

    def __post_init__(self) -> None:
        super().__post_init__()
        if self.is_set("deprecated_legacy_json_field_conflicts"):
            warnings.warn("EnumOptions.deprecated_legacy_json_field_conflicts is deprecated", DeprecationWarning)


default_message_pool.register_message("google.protobuf", "EnumOptions", EnumOptions)


@dataclass(eq=False, repr=False)
class EnumValueDescriptorProto(aristaproto.Message):
    """
    Describes a value within an enum.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    number: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)

    options: "EnumValueOptions | None" = aristaproto.field(3, aristaproto.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("google.protobuf", "EnumValueDescriptorProto", EnumValueDescriptorProto)


@dataclass(eq=False, repr=False)
class EnumValueOptions(aristaproto.Message):
    deprecated: "bool" = aristaproto.field(1, aristaproto.TYPE_BOOL)
    """
    Is this enum value deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for the enum value, or it will be completely ignored; in the very least,
    this is a formalization for deprecating enum values.
    """

    features: "FeatureSet | None" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    debug_redact: "bool" = aristaproto.field(3, aristaproto.TYPE_BOOL)
    """
    Indicate that fields annotated with this enum value should not be printed
    out when using debug formats, e.g. when the field contains sensitive
    credentials.
    """

    feature_support: "FieldOptionsFeatureSupport | None" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Information about the support window of a feature value.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """


default_message_pool.register_message("google.protobuf", "EnumValueOptions", EnumValueOptions)


@dataclass(eq=False, repr=False)
class ExtensionRangeOptions(aristaproto.Message):
    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """

    declaration: "list[ExtensionRangeOptionsDeclaration]" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    For external users: DO NOT USE. We are in the process of open sourcing
    extension declaration and executing internal cleanups before it can be
    used externally.
    """

    features: "FeatureSet | None" = aristaproto.field(50, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    """

    verification: "ExtensionRangeOptionsVerificationState" = aristaproto.field(
        3, aristaproto.TYPE_ENUM, default_factory=lambda: ExtensionRangeOptionsVerificationState(0)
    )
    """
    The verification state of the range.
    TODO: flip the default to DECLARATION once all empty ranges
    are marked as UNVERIFIED.
    """


default_message_pool.register_message("google.protobuf", "ExtensionRangeOptions", ExtensionRangeOptions)


@dataclass(eq=False, repr=False)
class ExtensionRangeOptionsDeclaration(aristaproto.Message):
    number: "int" = aristaproto.field(1, aristaproto.TYPE_INT32)
    """
    The extension number declared within the extension range.
    """

    full_name: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    The fully-qualified name of the extension field. There must be a leading
    dot in front of the full name.
    """

    type: "str" = aristaproto.field(3, aristaproto.TYPE_STRING)
    """
    The fully-qualified type name of the extension field. Unlike
    Metadata.type, Declaration.type must have a leading dot for messages
    and enums.
    """

    reserved: "bool" = aristaproto.field(5, aristaproto.TYPE_BOOL)
    """
    If true, indicates that the number is reserved in the extension range,
    and any extension field with the number will fail to compile. Set this
    when a declared extension field is deleted.
    """

    repeated: "bool" = aristaproto.field(6, aristaproto.TYPE_BOOL)
    """
    If true, indicates that the extension must be defined as repeated.
    Otherwise the extension must be defined as optional.
    """


default_message_pool.register_message("google.protobuf", "ExtensionRangeOptions.Declaration", ExtensionRangeOptionsDeclaration)


@dataclass(eq=False, repr=False)
class FeatureSet(aristaproto.Message):
    """
    ===================================================================
    Features

    TODO Enums in C++ gencode (and potentially other languages) are
    not well scoped.  This means that each of the feature enums below can clash
    with each other.  The short names we've chosen maximize call-site
    readability, but leave us very open to this scenario.  A future feature will
    be designed and implemented to handle this, hopefully before we ever hit a
    conflict here.
    """

    field_presence: "FeatureSetFieldPresence" = aristaproto.field(1, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetFieldPresence(0))

    enum_type: "FeatureSetEnumType" = aristaproto.field(2, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetEnumType(0))

    repeated_field_encoding: "FeatureSetRepeatedFieldEncoding" = aristaproto.field(
        3, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetRepeatedFieldEncoding(0)
    )

    utf8_validation: "FeatureSetUtf8Validation" = aristaproto.field(4, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetUtf8Validation(0))

    message_encoding: "FeatureSetMessageEncoding" = aristaproto.field(5, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetMessageEncoding(0))

    json_format: "FeatureSetJsonFormat" = aristaproto.field(6, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetJsonFormat(0))

    enforce_naming_style: "FeatureSetEnforceNamingStyle" = aristaproto.field(7, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetEnforceNamingStyle(0))

    default_symbol_visibility: "FeatureSetVisibilityFeatureDefaultSymbolVisibility" = aristaproto.field(
        8, aristaproto.TYPE_ENUM, default_factory=lambda: FeatureSetVisibilityFeatureDefaultSymbolVisibility(0)
    )


default_message_pool.register_message("google.protobuf", "FeatureSet", FeatureSet)


@dataclass(eq=False, repr=False)
class FeatureSetVisibilityFeature(aristaproto.Message):
    pass


default_message_pool.register_message("google.protobuf", "FeatureSet.VisibilityFeature", FeatureSetVisibilityFeature)


@dataclass(eq=False, repr=False)
class FeatureSetDefaults(aristaproto.Message):
    """
    A compiled specification for the defaults of a set of features.  These
    messages are generated from FeatureSet extensions and can be used to seed
    feature resolution. The resolution with this object becomes a simple search
    for the closest matching edition, followed by proto merges.
    """

    defaults: "list[FeatureSetDefaultsFeatureSetEditionDefault]" = aristaproto.field(1, aristaproto.TYPE_MESSAGE, repeated=True)

    minimum_edition: "Edition" = aristaproto.field(4, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The minimum supported edition (inclusive) when this was constructed.
    Editions before this will not have defaults.
    """

    maximum_edition: "Edition" = aristaproto.field(5, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The maximum known edition (inclusive) when this was constructed. Editions
    after this will not have reliable defaults.
    """


default_message_pool.register_message("google.protobuf", "FeatureSetDefaults", FeatureSetDefaults)


@dataclass(eq=False, repr=False)
class FeatureSetDefaultsFeatureSetEditionDefault(aristaproto.Message):
    """
    A map from every known edition with a unique set of defaults to its
    defaults. Not all editions may be contained here.  For a given edition,
    the defaults at the closest matching edition ordered at or before it should
    be used.  This field must be in strict ascending order by edition.
    """

    edition: "Edition" = aristaproto.field(3, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))

    overridable_features: "FeatureSet | None" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Defaults of features that can be overridden in this edition.
    """

    fixed_features: "FeatureSet | None" = aristaproto.field(5, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Defaults of features that can't be overridden in this edition.
    """


default_message_pool.register_message("google.protobuf", "FeatureSetDefaults.FeatureSetEditionDefault", FeatureSetDefaultsFeatureSetEditionDefault)


@dataclass(eq=False, repr=False)
class FieldDescriptorProto(aristaproto.Message):
    """
    Describes a field within a message.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    number: "int" = aristaproto.field(3, aristaproto.TYPE_INT32)

    label: "FieldDescriptorProtoLabel" = aristaproto.field(4, aristaproto.TYPE_ENUM, default_factory=lambda: FieldDescriptorProtoLabel(0))

    type: "FieldDescriptorProtoType" = aristaproto.field(5, aristaproto.TYPE_ENUM, default_factory=lambda: FieldDescriptorProtoType(0))
    """
    If type_name is set, this need not be set.  If both this and type_name
    are set, this must be one of TYPE_ENUM, TYPE_MESSAGE or TYPE_GROUP.
    """

    type_name: "str" = aristaproto.field(6, aristaproto.TYPE_STRING)
    """
    For message and enum types, this is the name of the type.  If the name
    starts with a '.', it is fully-qualified.  Otherwise, C++-like scoping
    rules are used to find the type (i.e. first the nested types within this
    message are searched, then within the parent, on up to the root
    namespace).
    """

    extendee: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    For extensions, this is the name of the type being extended.  It is
    resolved in the same manner as type_name.
    """

    default_value: "str" = aristaproto.field(7, aristaproto.TYPE_STRING)
    """
    For numeric types, contains the original text representation of the value.
    For booleans, "true" or "false".
    For strings, contains the default text contents (not escaped in any way).
    For bytes, contains the C escaped value.  All bytes >= 128 are escaped.
    """

    oneof_index: "int" = aristaproto.field(9, aristaproto.TYPE_INT32)
    """
    If set, gives the index of a oneof in the containing type's oneof_decl
    list.  This field is a member of that oneof.
    """

    json_name: "str" = aristaproto.field(10, aristaproto.TYPE_STRING)
    """
    JSON name of this field. The value is set by protocol compiler. If the
    user has set a "json_name" option on this field, that option's value
    will be used. Otherwise, it's deduced from the field's name by converting
    it to camelCase.
    """

    options: "FieldOptions | None" = aristaproto.field(8, aristaproto.TYPE_MESSAGE, optional=True)

    proto3_optional: "bool" = aristaproto.field(17, aristaproto.TYPE_BOOL)
    """
    If true, this is a proto3 "optional". When a proto3 field is optional, it
    tracks presence regardless of field type.

    When proto3_optional is true, this field must belong to a oneof to signal
    to old proto3 clients that presence is tracked for this field. This oneof
    is known as a "synthetic" oneof, and this field must be its sole member
    (each proto3 optional field gets its own synthetic oneof). Synthetic oneofs
    exist in the descriptor only, and do not generate any API. Synthetic oneofs
    must be ordered after all "real" oneofs.

    For message fields, proto3_optional doesn't create any semantic change,
    since non-repeated message fields always track presence. However it still
    indicates the semantic detail of whether the user wrote "optional" or not.
    This can be useful for round-tripping the .proto file. For consistency we
    give message fields a synthetic oneof also, even though it is not required
    to track presence. This is especially important because the parser can't
    tell if a field is a message or an enum, so it must always create a
    synthetic oneof.

    Proto2 optional fields do not set this flag, because they already indicate
    optional with `LABEL_OPTIONAL`.
    """


default_message_pool.register_message("google.protobuf", "FieldDescriptorProto", FieldDescriptorProto)


@dataclass(eq=False, repr=False)
class FieldOptions(aristaproto.Message):
    ctype: "FieldOptionsCType" = aristaproto.field(1, aristaproto.TYPE_ENUM, default_factory=lambda: FieldOptionsCType(0))
    """
    NOTE: ctype is deprecated. Use `features.(pb.cpp).string_type` instead.
    The ctype option instructs the C++ code generator to use a different
    representation of the field than it normally would.  See the specific
    options below.  This option is only implemented to support use of
    [ctype=CORD] and [ctype=STRING] (the default) on non-repeated fields of
    type "bytes" in the open source release.
    TODO: make ctype actually deprecated.
    """

    packed: "bool" = aristaproto.field(2, aristaproto.TYPE_BOOL)
    """
    The packed option can be enabled for repeated primitive fields to enable
    a more efficient representation on the wire. Rather than repeatedly
    writing the tag and type for each element, the entire array is encoded as
    a single length-delimited blob. In proto3, only explicit setting it to
    false will avoid using packed encoding.  This option is prohibited in
    Editions, but the `repeated_field_encoding` feature can be used to control
    the behavior.
    """

    jstype: "FieldOptionsJsType" = aristaproto.field(6, aristaproto.TYPE_ENUM, default_factory=lambda: FieldOptionsJsType(0))
    """
    The jstype option determines the JavaScript type used for values of the
    field.  The option is permitted only for 64 bit integral and fixed types
    (int64, uint64, sint64, fixed64, sfixed64).  A field with jstype JS_STRING
    is represented as JavaScript string, which avoids loss of precision that
    can happen when a large value is converted to a floating point JavaScript.
    Specifying JS_NUMBER for the jstype causes the generated JavaScript code to
    use the JavaScript "number" type.  The behavior of the default option
    JS_NORMAL is implementation dependent.

    This option is an enum to permit additional types to be added, e.g.
    goog.math.Integer.
    """

    lazy: "bool" = aristaproto.field(5, aristaproto.TYPE_BOOL)
    """
    Should this field be parsed lazily?  Lazy applies only to message-type
    fields.  It means that when the outer message is initially parsed, the
    inner message's contents will not be parsed but instead stored in encoded
    form.  The inner message will actually be parsed when it is first accessed.

    This is only a hint.  Implementations are free to choose whether to use
    eager or lazy parsing regardless of the value of this option.  However,
    setting this option true suggests that the protocol author believes that
    using lazy parsing on this field is worth the additional bookkeeping
    overhead typically needed to implement it.

    This option does not affect the public interface of any generated code;
    all method signatures remain the same.  Furthermore, thread-safety of the
    interface is not affected by this option; const methods remain safe to
    call from multiple threads concurrently, while non-const methods continue
    to require exclusive access.

    Note that lazy message fields are still eagerly verified to check
    ill-formed wireformat or missing required fields. Calling IsInitialized()
    on the outer message would fail if the inner message has missing required
    fields. Failed verification would result in parsing failure (except when
    uninitialized messages are acceptable).
    """

    unverified_lazy: "bool" = aristaproto.field(15, aristaproto.TYPE_BOOL)
    """
    unverified_lazy does no correctness checks on the byte stream. This should
    only be used where lazy with verification is prohibitive for performance
    reasons.
    """

    deprecated: "bool" = aristaproto.field(3, aristaproto.TYPE_BOOL)
    """
    Is this field deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for accessors, or it will be completely ignored; in the very least, this
    is a formalization for deprecating fields.
    """

    weak: "bool" = aristaproto.field(10, aristaproto.TYPE_BOOL)
    """
    DEPRECATED. DO NOT USE!
    For Google-internal migration only. Do not use.
    """

    debug_redact: "bool" = aristaproto.field(16, aristaproto.TYPE_BOOL)
    """
    Indicate that the field value should not be printed out when using debug
    formats, e.g. when the field contains sensitive credentials.
    """

    retention: "FieldOptionsOptionRetention" = aristaproto.field(17, aristaproto.TYPE_ENUM, default_factory=lambda: FieldOptionsOptionRetention(0))

    targets: "list[FieldOptionsOptionTargetType]" = aristaproto.field(19, aristaproto.TYPE_ENUM, repeated=True)

    edition_defaults: "list[FieldOptionsEditionDefault]" = aristaproto.field(20, aristaproto.TYPE_MESSAGE, repeated=True)

    features: "FeatureSet | None" = aristaproto.field(21, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    feature_support: "FieldOptionsFeatureSupport | None" = aristaproto.field(22, aristaproto.TYPE_MESSAGE, optional=True)

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """

    def __post_init__(self) -> None:
        super().__post_init__()
        if self.is_set("weak"):
            warnings.warn("FieldOptions.weak is deprecated", DeprecationWarning)


default_message_pool.register_message("google.protobuf", "FieldOptions", FieldOptions)


@dataclass(eq=False, repr=False)
class FieldOptionsEditionDefault(aristaproto.Message):
    edition: "Edition" = aristaproto.field(3, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))

    value: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    Textproto value.
    """


default_message_pool.register_message("google.protobuf", "FieldOptions.EditionDefault", FieldOptionsEditionDefault)


@dataclass(eq=False, repr=False)
class FieldOptionsFeatureSupport(aristaproto.Message):
    """
    Information about the support window of a feature.
    """

    edition_introduced: "Edition" = aristaproto.field(1, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The edition that this feature was first available in.  In editions
    earlier than this one, the default assigned to EDITION_LEGACY will be
    used, and proto files will not be able to override it.
    """

    edition_deprecated: "Edition" = aristaproto.field(2, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The edition this feature becomes deprecated in.  Using this after this
    edition may trigger warnings.
    """

    deprecation_warning: "str" = aristaproto.field(3, aristaproto.TYPE_STRING)
    """
    The deprecation warning text if this feature is used after the edition it
    was marked deprecated in.
    """

    edition_removed: "Edition" = aristaproto.field(4, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The edition this feature is no longer available in.  In editions after
    this one, the last default assigned will be used, and proto files will
    not be able to override it.
    """


default_message_pool.register_message("google.protobuf", "FieldOptions.FeatureSupport", FieldOptionsFeatureSupport)


@dataclass(eq=False, repr=False)
class FileDescriptorProto(aristaproto.Message):
    """
    Describes a complete .proto file.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)
    """
    file name, relative to root of source tree
    """

    package: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    e.g. "foo", "foo.bar", etc.
    """

    dependency: "list[str]" = aristaproto.field(3, aristaproto.TYPE_STRING, repeated=True)
    """
    Names of files imported by this file.
    """

    public_dependency: "list[int]" = aristaproto.field(10, aristaproto.TYPE_INT32, repeated=True)
    """
    Indexes of the public imported files in the dependency list above.
    """

    weak_dependency: "list[int]" = aristaproto.field(11, aristaproto.TYPE_INT32, repeated=True)
    """
    Indexes of the weak imported files in the dependency list.
    For Google-internal migration only. Do not use.
    """

    option_dependency: "list[str]" = aristaproto.field(15, aristaproto.TYPE_STRING, repeated=True)
    """
    Names of files imported by this file purely for the purpose of providing
    option extensions. These are excluded from the dependency list above.
    """

    message_type: "list[DescriptorProto]" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    All top-level definitions in this file.
    """

    enum_type: "list[EnumDescriptorProto]" = aristaproto.field(5, aristaproto.TYPE_MESSAGE, repeated=True)

    service: "list[ServiceDescriptorProto]" = aristaproto.field(6, aristaproto.TYPE_MESSAGE, repeated=True)

    extension: "list[FieldDescriptorProto]" = aristaproto.field(7, aristaproto.TYPE_MESSAGE, repeated=True)

    options: "FileOptions | None" = aristaproto.field(8, aristaproto.TYPE_MESSAGE, optional=True)

    source_code_info: "SourceCodeInfo | None" = aristaproto.field(9, aristaproto.TYPE_MESSAGE, optional=True)
    """
    This field contains optional information about the original source code.
    You may safely remove this entire field without harming runtime
    functionality of the descriptors -- the information is needed only by
    development tools.
    """

    syntax: "str" = aristaproto.field(12, aristaproto.TYPE_STRING)
    """
    The syntax of the proto file.
    The supported values are "proto2", "proto3", and "editions".

    If `edition` is present, this value must be "editions".
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    edition: "Edition" = aristaproto.field(14, aristaproto.TYPE_ENUM, default_factory=lambda: Edition(0))
    """
    The edition of the proto file.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """


default_message_pool.register_message("google.protobuf", "FileDescriptorProto", FileDescriptorProto)


@dataclass(eq=False, repr=False)
class FileDescriptorSet(aristaproto.Message):
    """
    The protocol compiler can output a FileDescriptorSet containing the .proto
    files it parses.
    """

    file: "list[FileDescriptorProto]" = aristaproto.field(1, aristaproto.TYPE_MESSAGE, repeated=True)


default_message_pool.register_message("google.protobuf", "FileDescriptorSet", FileDescriptorSet)


@dataclass(eq=False, repr=False)
class FileOptions(aristaproto.Message):
    """
    ===================================================================
    Options

    Each of the definitions above may have "options" attached.  These are
    just annotations which may cause code to be generated slightly differently
    or may contain hints for code that manipulates protocol messages.

    Clients may define custom options as extensions of the *Options messages.
    These extensions may not yet be known at parsing time, so the parser cannot
    store the values in them.  Instead it stores them in a field in the *Options
    message called uninterpreted_option. This field must have the same name
    across all *Options messages. We then use this field to populate the
    extensions when we build a descriptor, at which point all protos have been
    parsed and so all extensions are known.

    Extension numbers for custom options may be chosen as follows:
    * For options which will only be used within a single application or
      organization, or for experimental options, use field numbers 50000
      through 99999.  It is up to you to ensure that you do not use the
      same number for multiple options.
    * For options which will be published and used publicly by multiple
      independent entities, e-mail protobuf-global-extension-registry@google.com
      to reserve extension numbers. Simply provide your project name (e.g.
      Objective-C plugin) and your project website (if available) -- there's no
      need to explain how you intend to use them. Usually you only need one
      extension number. You can declare multiple options with only one extension
      number by putting them in a sub-message. See the Custom Options section of
      the docs for examples:
      https://developers.google.com/protocol-buffers/docs/proto#options
      If this turns out to be popular, a web service will be set up
      to automatically assign option numbers.
    """

    java_package: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)
    """
    Sets the Java package where classes generated from this .proto will be
    placed.  By default, the proto package is used, but this is often
    inappropriate because proto packages do not normally start with backwards
    domain names.
    """

    java_outer_classname: "str" = aristaproto.field(8, aristaproto.TYPE_STRING)
    """
    Controls the name of the wrapper Java class generated for the .proto file.
    That class will always contain the .proto file's getDescriptor() method as
    well as any top-level extensions defined in the .proto file.
    If java_multiple_files is disabled, then all the other classes from the
    .proto file will be nested inside the single wrapper outer class.
    """

    java_multiple_files: "bool" = aristaproto.field(10, aristaproto.TYPE_BOOL)
    """
    If enabled, then the Java code generator will generate a separate .java
    file for each top-level message, enum, and service defined in the .proto
    file.  Thus, these types will *not* be nested inside the wrapper class
    named by java_outer_classname.  However, the wrapper class will still be
    generated to contain the file's getDescriptor() method as well as any
    top-level extensions defined in the file.
    """

    java_generate_equals_and_hash: "bool" = aristaproto.field(20, aristaproto.TYPE_BOOL)
    """
    This option does nothing.
    """

    java_string_check_utf8: "bool" = aristaproto.field(27, aristaproto.TYPE_BOOL)
    """
    A proto2 file can set this to true to opt in to UTF-8 checking for Java,
    which will throw an exception if invalid UTF-8 is parsed from the wire or
    assigned to a string field.

    TODO: clarify exactly what kinds of field types this option
    applies to, and update these docs accordingly.

    Proto3 files already perform these checks. Setting the option explicitly to
    false has no effect: it cannot be used to opt proto3 files out of UTF-8
    checks.
    """

    optimize_for: "FileOptionsOptimizeMode" = aristaproto.field(9, aristaproto.TYPE_ENUM, default_factory=lambda: FileOptionsOptimizeMode(0))

    go_package: "str" = aristaproto.field(11, aristaproto.TYPE_STRING)
    """
    Sets the Go package where structs generated from this .proto will be
    placed. If omitted, the Go package will be derived from the following:
      - The basename of the package import path, if provided.
      - Otherwise, the package statement in the .proto file, if present.
      - Otherwise, the basename of the .proto file, without extension.
    """

    cc_generic_services: "bool" = aristaproto.field(16, aristaproto.TYPE_BOOL)
    """
    Should generic services be generated in each language?  "Generic" services
    are not specific to any particular RPC system.  They are generated by the
    main code generators in each language (without additional plugins).
    Generic services were the only kind of service generation supported by
    early versions of google.protobuf.

    Generic services are now considered deprecated in favor of using plugins
    that generate code specific to your particular RPC system.  Therefore,
    these default to false.  Old code which depends on generic services should
    explicitly set them to true.
    """

    java_generic_services: "bool" = aristaproto.field(17, aristaproto.TYPE_BOOL)

    py_generic_services: "bool" = aristaproto.field(18, aristaproto.TYPE_BOOL)

    deprecated: "bool" = aristaproto.field(23, aristaproto.TYPE_BOOL)
    """
    Is this file deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for everything in the file, or it will be completely ignored; in the very
    least, this is a formalization for deprecating files.
    """

    cc_enable_arenas: "bool" = aristaproto.field(31, aristaproto.TYPE_BOOL)
    """
    Enables the use of arenas for the proto messages in this file. This applies
    only to generated classes for C++.
    """

    objc_class_prefix: "str" = aristaproto.field(36, aristaproto.TYPE_STRING)
    """
    Sets the objective c class prefix which is prepended to all objective c
    generated classes from this .proto. There is no default.
    """

    csharp_namespace: "str" = aristaproto.field(37, aristaproto.TYPE_STRING)
    """
    Namespace for generated classes; defaults to the package.
    """

    swift_prefix: "str" = aristaproto.field(39, aristaproto.TYPE_STRING)
    """
    By default Swift generators will take the proto package and CamelCase it
    replacing '.' with underscore and use that to prefix the types/symbols
    defined. When this options is provided, they will use this value instead
    to prefix the types/symbols defined.
    """

    php_class_prefix: "str" = aristaproto.field(40, aristaproto.TYPE_STRING)
    """
    Sets the php class prefix which is prepended to all php generated classes
    from this .proto. Default is empty.
    """

    php_namespace: "str" = aristaproto.field(41, aristaproto.TYPE_STRING)
    """
    Use this option to change the namespace of php generated classes. Default
    is empty. When this option is empty, the package name will be used for
    determining the namespace.
    """

    php_metadata_namespace: "str" = aristaproto.field(44, aristaproto.TYPE_STRING)
    """
    Use this option to change the namespace of php generated metadata classes.
    Default is empty. When this option is empty, the proto file name will be
    used for determining the namespace.
    """

    ruby_package: "str" = aristaproto.field(45, aristaproto.TYPE_STRING)
    """
    Use this option to change the package of ruby generated classes. Default
    is empty. When this option is not set, the package name will be used for
    determining the ruby package.
    """

    features: "FeatureSet | None" = aristaproto.field(50, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here.
    See the documentation for the "Options" section above.
    """

    def __post_init__(self) -> None:
        super().__post_init__()
        if self.is_set("java_generate_equals_and_hash"):
            warnings.warn("FileOptions.java_generate_equals_and_hash is deprecated", DeprecationWarning)


default_message_pool.register_message("google.protobuf", "FileOptions", FileOptions)


@dataclass(eq=False, repr=False)
class FloatValue(aristaproto.Message):
    """
    Wrapper message for `float`.

    The JSON representation for `FloatValue` is JSON number.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "float" = aristaproto.field(1, aristaproto.TYPE_FLOAT)
    """
    The float value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, float):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: float) -> "FloatValue":
        return FloatValue(value=wrapped)

    def to_wrapped(self) -> float:
        return self.value


default_message_pool.register_message("google.protobuf", "FloatValue", FloatValue)


@dataclass(eq=False, repr=False)
class GeneratedCodeInfo(aristaproto.Message):
    """
    Describes the relationship between generated code and its original source
    file. A GeneratedCodeInfo message is associated with only one generated
    source file, but may contain references to different source .proto files.
    """

    annotation: "list[GeneratedCodeInfoAnnotation]" = aristaproto.field(1, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    An Annotation connects some span of text in generated code to an element
    of its generating .proto file.
    """


default_message_pool.register_message("google.protobuf", "GeneratedCodeInfo", GeneratedCodeInfo)


@dataclass(eq=False, repr=False)
class GeneratedCodeInfoAnnotation(aristaproto.Message):
    path: "list[int]" = aristaproto.field(1, aristaproto.TYPE_INT32, repeated=True)
    """
    Identifies the element in the original source .proto file. This field
    is formatted the same as SourceCodeInfo.Location.path.
    """

    source_file: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    Identifies the filesystem path to the original source .proto.
    """

    begin: "int" = aristaproto.field(3, aristaproto.TYPE_INT32)
    """
    Identifies the starting offset in bytes in the generated code
    that relates to the identified object.
    """

    end: "int" = aristaproto.field(4, aristaproto.TYPE_INT32)
    """
    Identifies the ending offset in bytes in the generated code that
    relates to the identified object. The end offset should be one past
    the last relevant byte (so the length of the text = end - begin).
    """

    semantic: "GeneratedCodeInfoAnnotationSemantic" = aristaproto.field(
        5, aristaproto.TYPE_ENUM, default_factory=lambda: GeneratedCodeInfoAnnotationSemantic(0)
    )


default_message_pool.register_message("google.protobuf", "GeneratedCodeInfo.Annotation", GeneratedCodeInfoAnnotation)


@dataclass(eq=False, repr=False)
class Int32Value(aristaproto.Message):
    """
    Wrapper message for `int32`.

    The JSON representation for `Int32Value` is JSON number.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "int" = aristaproto.field(1, aristaproto.TYPE_INT32)
    """
    The int32 value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, int):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: int) -> "Int32Value":
        return Int32Value(value=wrapped)

    def to_wrapped(self) -> int:
        return self.value


default_message_pool.register_message("google.protobuf", "Int32Value", Int32Value)


@dataclass(eq=False, repr=False)
class Int64Value(aristaproto.Message):
    """
    Wrapper message for `int64`.

    The JSON representation for `Int64Value` is JSON string.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "int" = aristaproto.field(1, aristaproto.TYPE_INT64)
    """
    The int64 value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, int):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: int) -> "Int64Value":
        return Int64Value(value=wrapped)

    def to_wrapped(self) -> int:
        return self.value


default_message_pool.register_message("google.protobuf", "Int64Value", Int64Value)


@dataclass(eq=False, repr=False)
class MessageOptions(aristaproto.Message):
    message_set_wire_format: "bool" = aristaproto.field(1, aristaproto.TYPE_BOOL)
    """
    Set true to use the old proto1 MessageSet wire format for extensions.
    This is provided for backwards-compatibility with the MessageSet wire
    format.  You should not use this for any other reason:  It's less
    efficient, has fewer features, and is more complicated.

    The message must be defined exactly as follows:
      message Foo {
        option message_set_wire_format = true;
        extensions 4 to max;
      }
    Note that the message cannot have any defined fields; MessageSets only
    have extensions.

    All extensions of your type must be singular messages; e.g. they cannot
    be int32s, enums, or repeated messages.

    Because this is an option, the above two restrictions are not enforced by
    the protocol compiler.
    """

    no_standard_descriptor_accessor: "bool" = aristaproto.field(2, aristaproto.TYPE_BOOL)
    """
    Disables the generation of the standard "descriptor()" accessor, which can
    conflict with a field of the same name.  This is meant to make migration
    from proto1 easier; new code should avoid fields named "descriptor".
    """

    deprecated: "bool" = aristaproto.field(3, aristaproto.TYPE_BOOL)
    """
    Is this message deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for the message, or it will be completely ignored; in the very least,
    this is a formalization for deprecating messages.
    """

    map_entry: "bool" = aristaproto.field(7, aristaproto.TYPE_BOOL)
    """
    Whether the message is an automatically generated map entry type for the
    maps field.

    For maps fields:
        map<KeyType, ValueType> map_field = 1;
    The parsed descriptor looks like:
        message MapFieldEntry {
            option map_entry = true;
            optional KeyType key = 1;
            optional ValueType value = 2;
        }
        repeated MapFieldEntry map_field = 1;

    Implementations may choose not to generate the map_entry=true message, but
    use a native map in the target language to hold the keys and values.
    The reflection APIs in such implementations still need to work as
    if the field is a repeated message field.

    NOTE: Do not set the option in .proto files. Always use the maps syntax
    instead. The option should only be implicitly set by the proto compiler
    parser.
    """

    deprecated_legacy_json_field_conflicts: "bool" = aristaproto.field(11, aristaproto.TYPE_BOOL)
    """
    Enable the legacy handling of JSON field name conflicts.  This lowercases
    and strips underscored from the fields before comparison in proto3 only.
    The new behavior takes `json_name` into account and applies to proto2 as
    well.

    This should only be used as a temporary measure against broken builds due
    to the change in behavior for JSON field name conflicts.

    TODO This is legacy behavior we plan to remove once downstream
    teams have had time to migrate.
    """

    features: "FeatureSet | None" = aristaproto.field(12, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """

    def __post_init__(self) -> None:
        super().__post_init__()
        if self.is_set("deprecated_legacy_json_field_conflicts"):
            warnings.warn("MessageOptions.deprecated_legacy_json_field_conflicts is deprecated", DeprecationWarning)


default_message_pool.register_message("google.protobuf", "MessageOptions", MessageOptions)


@dataclass(eq=False, repr=False)
class MethodDescriptorProto(aristaproto.Message):
    """
    Describes a method of a service.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    input_type: "str" = aristaproto.field(2, aristaproto.TYPE_STRING)
    """
    Input and output type names.  These are resolved in the same way as
    FieldDescriptorProto.type_name, but must refer to a message type.
    """

    output_type: "str" = aristaproto.field(3, aristaproto.TYPE_STRING)

    options: "MethodOptions | None" = aristaproto.field(4, aristaproto.TYPE_MESSAGE, optional=True)

    client_streaming: "bool" = aristaproto.field(5, aristaproto.TYPE_BOOL)
    """
    Identifies if client streams multiple client messages
    """

    server_streaming: "bool" = aristaproto.field(6, aristaproto.TYPE_BOOL)
    """
    Identifies if server streams multiple server messages
    """


default_message_pool.register_message("google.protobuf", "MethodDescriptorProto", MethodDescriptorProto)


@dataclass(eq=False, repr=False)
class MethodOptions(aristaproto.Message):
    deprecated: "bool" = aristaproto.field(33, aristaproto.TYPE_BOOL)
    """
    Note:  Field numbers 1 through 32 are reserved for Google's internal RPC
      framework.  We apologize for hoarding these numbers to ourselves, but
      we were already using them long before we decided to release Protocol
      Buffers.

    Is this method deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for the method, or it will be completely ignored; in the very least,
    this is a formalization for deprecating methods.
    """

    idempotency_level: "MethodOptionsIdempotencyLevel" = aristaproto.field(34, aristaproto.TYPE_ENUM, default_factory=lambda: MethodOptionsIdempotencyLevel(0))

    features: "FeatureSet | None" = aristaproto.field(35, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """


default_message_pool.register_message("google.protobuf", "MethodOptions", MethodOptions)


@dataclass(eq=False, repr=False)
class OneofDescriptorProto(aristaproto.Message):
    """
    Describes a oneof.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    options: "OneofOptions | None" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("google.protobuf", "OneofDescriptorProto", OneofDescriptorProto)


@dataclass(eq=False, repr=False)
class OneofOptions(aristaproto.Message):
    features: "FeatureSet | None" = aristaproto.field(1, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """


default_message_pool.register_message("google.protobuf", "OneofOptions", OneofOptions)


@dataclass(eq=False, repr=False)
class ServiceDescriptorProto(aristaproto.Message):
    """
    Describes a service.
    """

    name: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    method: "list[MethodDescriptorProto]" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, repeated=True)

    options: "ServiceOptions | None" = aristaproto.field(3, aristaproto.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("google.protobuf", "ServiceDescriptorProto", ServiceDescriptorProto)


@dataclass(eq=False, repr=False)
class ServiceOptions(aristaproto.Message):
    features: "FeatureSet | None" = aristaproto.field(34, aristaproto.TYPE_MESSAGE, optional=True)
    """
    Any features defined in the specific edition.
    WARNING: This field should only be used by protobuf plugins or special
    cases like the proto compiler. Other uses are discouraged and
    developers should rely on the protoreflect APIs for their client language.
    """

    deprecated: "bool" = aristaproto.field(33, aristaproto.TYPE_BOOL)
    """
    Note:  Field numbers 1 through 32 are reserved for Google's internal RPC
      framework.  We apologize for hoarding these numbers to ourselves, but
      we were already using them long before we decided to release Protocol
      Buffers.

    Is this service deprecated?
    Depending on the target platform, this can emit Deprecated annotations
    for the service, or it will be completely ignored; in the very least,
    this is a formalization for deprecating services.
    """

    uninterpreted_option: "list[UninterpretedOption]" = aristaproto.field(999, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    The parser stores options it doesn't recognize here. See above.
    """


default_message_pool.register_message("google.protobuf", "ServiceOptions", ServiceOptions)


@dataclass(eq=False, repr=False)
class SourceCodeInfo(aristaproto.Message):
    """
    ===================================================================
    Optional source code info

    Encapsulates information about the original source file from which a
    FileDescriptorProto was generated.
    """

    location: "list[SourceCodeInfoLocation]" = aristaproto.field(1, aristaproto.TYPE_MESSAGE, repeated=True)
    """
    A Location identifies a piece of source code in a .proto file which
    corresponds to a particular definition.  This information is intended
    to be useful to IDEs, code indexers, documentation generators, and similar
    tools.

    For example, say we have a file like:
      message Foo {
        optional string foo = 1;
      }
    Let's look at just the field definition:
      optional string foo = 1;
      ^       ^^     ^^  ^  ^^^
      a       bc     de  f  ghi
    We have the following locations:
      span   path               represents
      [a,i)  [ 4, 0, 2, 0 ]     The whole field definition.
      [a,b)  [ 4, 0, 2, 0, 4 ]  The label (optional).
      [c,d)  [ 4, 0, 2, 0, 5 ]  The type (string).
      [e,f)  [ 4, 0, 2, 0, 1 ]  The name (foo).
      [g,h)  [ 4, 0, 2, 0, 3 ]  The number (1).

    Notes:
    - A location may refer to a repeated field itself (i.e. not to any
      particular index within it).  This is used whenever a set of elements are
      logically enclosed in a single code segment.  For example, an entire
      extend block (possibly containing multiple extension definitions) will
      have an outer location whose path refers to the "extensions" repeated
      field without an index.
    - Multiple locations may have the same path.  This happens when a single
      logical declaration is spread out across multiple places.  The most
      obvious example is the "extend" block again -- there may be multiple
      extend blocks in the same scope, each of which will have the same path.
    - A location's span is not always a subset of its parent's span.  For
      example, the "extendee" of an extension declaration appears at the
      beginning of the "extend" block and is shared by all extensions within
      the block.
    - Just because a location's span is a subset of some other location's span
      does not mean that it is a descendant.  For example, a "group" defines
      both a type and a field in a single declaration.  Thus, the locations
      corresponding to the type and field and their components will overlap.
    - Code which tries to interpret locations should probably be designed to
      ignore those that it doesn't understand, as more types of locations could
      be recorded in the future.
    """


default_message_pool.register_message("google.protobuf", "SourceCodeInfo", SourceCodeInfo)


@dataclass(eq=False, repr=False)
class SourceCodeInfoLocation(aristaproto.Message):
    path: "list[int]" = aristaproto.field(1, aristaproto.TYPE_INT32, repeated=True)
    """
    Identifies which part of the FileDescriptorProto was defined at this
    location.

    Each element is a field number or an index.  They form a path from
    the root FileDescriptorProto to the place where the definition appears.
    For example, this path:
      [ 4, 3, 2, 7, 1 ]
    refers to:
      file.message_type(3)  // 4, 3
          .field(7)         // 2, 7
          .name()           // 1
    This is because FileDescriptorProto.message_type has field number 4:
      repeated DescriptorProto message_type = 4;
    and DescriptorProto.field has field number 2:
      repeated FieldDescriptorProto field = 2;
    and FieldDescriptorProto.name has field number 1:
      optional string name = 1;

    Thus, the above path gives the location of a field name.  If we removed
    the last element:
      [ 4, 3, 2, 7 ]
    this path refers to the whole field declaration (from the beginning
    of the label to the terminating semicolon).
    """

    span: "list[int]" = aristaproto.field(2, aristaproto.TYPE_INT32, repeated=True)
    """
    Always has exactly three or four elements: start line, start column,
    end line (optional, otherwise assumed same as start line), end column.
    These are packed into a single field for efficiency.  Note that line
    and column numbers are zero-based -- typically you will want to add
    1 to each before displaying to a user.
    """

    leading_comments: "str" = aristaproto.field(3, aristaproto.TYPE_STRING)
    """
    If this SourceCodeInfo represents a complete declaration, these are any
    comments appearing before and after the declaration which appear to be
    attached to the declaration.

    A series of line comments appearing on consecutive lines, with no other
    tokens appearing on those lines, will be treated as a single comment.

    leading_detached_comments will keep paragraphs of comments that appear
    before (but not connected to) the current element. Each paragraph,
    separated by empty lines, will be one comment element in the repeated
    field.

    Only the comment content is provided; comment markers (e.g. //) are
    stripped out.  For block comments, leading whitespace and an asterisk
    will be stripped from the beginning of each line other than the first.
    Newlines are included in the output.

    Examples:

      optional int32 foo = 1;  // Comment attached to foo.
      // Comment attached to bar.
      optional int32 bar = 2;

      optional string baz = 3;
      // Comment attached to baz.
      // Another line attached to baz.

      // Comment attached to moo.
      //
      // Another line attached to moo.
      optional double moo = 4;

      // Detached comment for corge. This is not leading or trailing comments
      // to moo or corge because there are blank lines separating it from
      // both.

      // Detached comment for corge paragraph 2.

      optional string corge = 5;
      /* Block comment attached
       * to corge.  Leading asterisks
       * will be removed. */
      /* Block comment attached to
       * grault. */
      optional int32 grault = 6;

      // ignored detached comments.
    """

    trailing_comments: "str" = aristaproto.field(4, aristaproto.TYPE_STRING)

    leading_detached_comments: "list[str]" = aristaproto.field(6, aristaproto.TYPE_STRING, repeated=True)


default_message_pool.register_message("google.protobuf", "SourceCodeInfo.Location", SourceCodeInfoLocation)


@dataclass(eq=False, repr=False)
class StringValue(aristaproto.Message):
    """
    Wrapper message for `string`.

    The JSON representation for `StringValue` is JSON string.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)
    """
    The string value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, str):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: str) -> "StringValue":
        return StringValue(value=wrapped)

    def to_wrapped(self) -> str:
        return self.value


default_message_pool.register_message("google.protobuf", "StringValue", StringValue)


@dataclass(eq=False, repr=False)
class Timestamp(aristaproto.Message):
    """
    A Timestamp represents a point in time independent of any time zone or local
    calendar, encoded as a count of seconds and fractions of seconds at
    nanosecond resolution. The count is relative to an epoch at UTC midnight on
    January 1, 1970, in the proleptic Gregorian calendar which extends the
    Gregorian calendar backwards to year one.

    All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
    second table is needed for interpretation, using a [24-hour linear
    smear](https://developers.google.com/time/smear).

    The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
    restricting to that range, we ensure that we can convert to and from [RFC
    3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

    # Examples

    Example 1: Compute Timestamp from POSIX `time()`.

        Timestamp timestamp;
        timestamp.set_seconds(time(NULL));
        timestamp.set_nanos(0);

    Example 2: Compute Timestamp from POSIX `gettimeofday()`.

        struct timeval tv;
        gettimeofday(&tv, NULL);

        Timestamp timestamp;
        timestamp.set_seconds(tv.tv_sec);
        timestamp.set_nanos(tv.tv_usec * 1000);

    Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

        FILETIME ft;
        GetSystemTimeAsFileTime(&ft);
        UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

        // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
        // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
        Timestamp timestamp;
        timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
        timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

    Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

        long millis = System.currentTimeMillis();

        Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
            .setNanos((int) ((millis % 1000) * 1000000)).build();

    Example 5: Compute Timestamp from Java `Instant.now()`.

        Instant now = Instant.now();

        Timestamp timestamp =
            Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                .setNanos(now.getNano()).build();

    Example 6: Compute Timestamp from current time in Python.

        timestamp = Timestamp()
        timestamp.GetCurrentTime()

    # JSON Mapping

    In JSON format, the Timestamp type is encoded as a string in the
    [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
    format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
    where {year} is always expressed using four digits while {month}, {day},
    {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
    seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
    are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
    is required. A proto3 JSON serializer should always use UTC (as indicated by
    "Z") when printing the Timestamp type and a proto3 JSON parser should be
    able to accept both UTC and other timezones (as indicated by an offset).

    For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
    01:30 UTC on January 15, 2017.

    In JavaScript, one can convert a Date object to this format using the
    standard
    [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
    method. In Python, a standard `datetime.datetime` object can be converted
    to this format using
    [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
    the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
    the Joda Time's [`ISODateTimeFormat.dateTime()`](
    http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
    ) to obtain a formatter capable of generating timestamps in this format.
    """

    seconds: "int" = aristaproto.field(1, aristaproto.TYPE_INT64)
    """
    Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must
    be between -315576000000 and 315576000000 inclusive (which corresponds to
    0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z).
    """

    nanos: "int" = aristaproto.field(2, aristaproto.TYPE_INT32)
    """
    Non-negative fractions of a second at nanosecond resolution. This field is
    the nanosecond portion of the duration, not an alternative to seconds.
    Negative second values with fractions must still have non-negative nanos
    values that count forward in time. Must be between 0 and 999,999,999
    inclusive.
    """

    @classmethod
    def from_datetime(cls, dt: datetime.datetime) -> Self:
        seconds, nanos = NanoDatetime.to_timestamp(dt)
        return cls(seconds, nanos)

    def to_datetime(self) -> datetime.datetime:
        return NanoDatetime.from_timestamp(self.seconds, self.nanos)

    @staticmethod
    def timestamp_to_json(dt: datetime.datetime) -> str:
        return NanoDatetime.to_json(dt)

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, str):
            return cls.from_datetime(NanoDatetime.from_rfc3339(value))

        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        # If the output format is PYTHON, we should have kept the wraped type without building the real class
        assert output_format == aristaproto.OutputFormat.PROTO_JSON

        return Timestamp.timestamp_to_json(self.to_datetime())

    @staticmethod
    def from_wrapped(wrapped: datetime.datetime) -> "Timestamp":
        return Timestamp.from_datetime(wrapped)

    def to_wrapped(self) -> datetime.datetime:
        return self.to_datetime()


default_message_pool.register_message("google.protobuf", "Timestamp", Timestamp)


@dataclass(eq=False, repr=False)
class UInt32Value(aristaproto.Message):
    """
    Wrapper message for `uint32`.

    The JSON representation for `UInt32Value` is JSON number.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "int" = aristaproto.field(1, aristaproto.TYPE_UINT32)
    """
    The uint32 value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, int):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: int) -> "UInt32Value":
        return UInt32Value(value=wrapped)

    def to_wrapped(self) -> int:
        return self.value


default_message_pool.register_message("google.protobuf", "UInt32Value", UInt32Value)


@dataclass(eq=False, repr=False)
class UInt64Value(aristaproto.Message):
    """
    Wrapper message for `uint64`.

    The JSON representation for `UInt64Value` is JSON string.

    Not recommended for use in new APIs, but still useful for legacy APIs and
    has no plan to be removed.
    """

    value: "int" = aristaproto.field(1, aristaproto.TYPE_UINT64)
    """
    The uint64 value.
    """

    @classmethod
    def from_dict(cls, value, *, ignore_unknown_fields: bool = False) -> Self:
        if isinstance(value, int):
            return cls(value=value)
        return super().from_dict(value, ignore_unknown_fields=ignore_unknown_fields)

    def to_dict(
        self,
        *,
        output_format: aristaproto.OutputFormat = aristaproto.OutputFormat.PROTO_JSON,
        casing: aristaproto.Casing = aristaproto.Casing.CAMEL,
        include_default_values: bool = False,
    ) -> dict[str, typing.Any] | typing.Any:
        return self.value

    @staticmethod
    def from_wrapped(wrapped: int) -> "UInt64Value":
        return UInt64Value(value=wrapped)

    def to_wrapped(self) -> int:
        return self.value


default_message_pool.register_message("google.protobuf", "UInt64Value", UInt64Value)


@dataclass(eq=False, repr=False)
class UninterpretedOption(aristaproto.Message):
    """
    A message representing a option the parser does not recognize. This only
    appears in options protos created by the compiler::Parser class.
    DescriptorPool resolves these when building Descriptor objects. Therefore,
    options protos in descriptor objects (e.g. returned by Descriptor::options(),
    or produced by Descriptor::CopyTo()) will never have UninterpretedOptions
    in them.
    """

    name: "list[UninterpretedOptionNamePart]" = aristaproto.field(2, aristaproto.TYPE_MESSAGE, repeated=True)

    identifier_value: "str" = aristaproto.field(3, aristaproto.TYPE_STRING)
    """
    The value of the uninterpreted option, in whatever type the tokenizer
    identified it as during parsing. Exactly one of these should be set.
    """

    positive_int_value: "int" = aristaproto.field(4, aristaproto.TYPE_UINT64)

    negative_int_value: "int" = aristaproto.field(5, aristaproto.TYPE_INT64)

    double_value: "float" = aristaproto.field(6, aristaproto.TYPE_DOUBLE)

    string_value: "bytes" = aristaproto.field(7, aristaproto.TYPE_BYTES)

    aggregate_value: "str" = aristaproto.field(8, aristaproto.TYPE_STRING)


default_message_pool.register_message("google.protobuf", "UninterpretedOption", UninterpretedOption)


@dataclass(eq=False, repr=False)
class UninterpretedOptionNamePart(aristaproto.Message):
    """
    The name of the uninterpreted option.  Each string represents a segment in
    a dot-separated name.  is_extension is true iff a segment represents an
    extension (denoted with parentheses in options specs in .proto files).
    E.g.,{ ["foo", false], ["bar.baz", true], ["moo", false] } represents
    "foo.(bar.baz).moo".
    """

    name_part: "str" = aristaproto.field(1, aristaproto.TYPE_STRING)

    is_extension: "bool" = aristaproto.field(2, aristaproto.TYPE_BOOL)


default_message_pool.register_message("google.protobuf", "UninterpretedOption.NamePart", UninterpretedOptionNamePart)
