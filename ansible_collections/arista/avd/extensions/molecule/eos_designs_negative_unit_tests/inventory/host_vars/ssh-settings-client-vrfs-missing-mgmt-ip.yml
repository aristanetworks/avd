type: l2leaf
l2leaf:
  nodes:
    - name: ssh-settings-client-vrfs-missing-mgmt-ip
      id: 1
      # mgmt_ip: 192.168.0.2/24
      # inband_mgmt_ip: 10.20.30.40/24
      # inband_mgmt_gateway: 10.20.30.1
      # inband_mgmt_vrf: INBANDMGMT

ssh_settings:
  client_vrfs:
    - name: use_mgmt_interface_vrf

expected_error_message: >-
  'ssh_settings.client_vrfs[name=use_mgmt_interface_vrf]' is set to 'use_mgmt_interface_vrf' but this node is missing 'mgmt_ip' or 'ipv6_mgmt_ip'.
