type: l2leaf
l2leaf:
  defaults:
  nodes:
    - name: aaa-settings-tacacs-group
      mgmt_ip: 192.168.0.2/24

aaa_settings:
  tacacs:
    servers:
      - host: agni.arista.com
        groups:
          - tacacs+
        cleartext_key: super-secret-key

expected_error_message: >-
  Server group 'tacacs+' is reserved by EOS and cannot be used. Please specify a different server group name.
