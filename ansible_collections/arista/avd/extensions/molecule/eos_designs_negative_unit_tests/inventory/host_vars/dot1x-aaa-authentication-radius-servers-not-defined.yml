---
type: l2leaf
l2leaf:
  defaults:
  nodes:
    - name: dot1x-aaa-authentication-radius-servers-not-defined
      mgmt_ip: 192.168.0.2/24

dot1x_settings:
  enabled: true

expected_error_message: >-
  At least one RADIUS server must be defined under `aaa_settings.radius.servers` for 802.1X authentication.
