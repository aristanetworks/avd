---
type: l2leaf

l2leaf: {}

default_mgmt_method: inband

snmp_settings:
  compute_local_engineid: true
  # missing inband IP to compute properly the engine ID
#
expected_error_message: >-
  'snmp_settings.local_engineid_ip' is set to 'use_inband_mgmt_interface' but this node is missing configuration for inband management.
