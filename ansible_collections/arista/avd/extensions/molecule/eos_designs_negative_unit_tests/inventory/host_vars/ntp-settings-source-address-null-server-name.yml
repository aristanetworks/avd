type: l2leaf
l2leaf:
  nodes:
    - name: ntp-settings-source-address-null-server-name
      id: 1
      mgmt_ip: 192.168.0.2/24

ntp_settings:
  server_vrf: use_mgmt_interface_vrf
  servers:
    - source_address: use_mgmt_interface_ipv4

expected_error_message: >-
  'ntp_settings.servers[].name' is required and cannot be null/none.
