type: l2leaf
l2leaf:
  nodes:
    - name: source-interfaces-ssh-client-missing-inband-mgmt-interface
      id: 1
      mgmt_ip: 192.168.0.2/24
      # inband_mgmt_ip: 10.20.30.40/24
      # inband_mgmt_gateway: 10.20.30.1
      # inband_mgmt_vrf: INBANDMGMT

ssh_settings:
  vrfs:
    - name: use_inband_mgmt_vrf
      enabled: true
      client_source_interface: use_inband_mgmt_interface

expected_error_message: >-
  Error during plugin 'arista.avd.eos_designs_structured_config' execution:
  'ssh_settings.vrfs[name=use_inband_mgmt_vrf]' is set to 'use_inband_mgmt_vrf' but this node is missing configuration for inband management.
