type: l2leaf
l2leaf:
  nodes:
    - name: source-interfaces-ssh-client-missing-inband-mgmt-interface
      id: 1
      mgmt_ip: 192.168.0.2/24
      # inband_mgmt_ip: 10.20.30.40/24
      # inband_mgmt_gateway: 10.20.30.1
      # inband_mgmt_vrf: INBANDMGMT

ssh_settings:
  client_source_interfaces:
    - vrf: default

expected_error_message: >-
  Unable to configure IP SSH Client source-interface for VRF 'default' since 'ssh_settings.client_source_interfaces[vrf=default].interface' is not set. Set 'interface' for non-management VRF values.
