type: l2leaf
l2leaf:
  nodes:
    - name: ssh-settings-client-vrfs-missing-source-interface
      id: 1
      mgmt_ip: 192.168.0.2/24
      # inband_mgmt_ip: 10.20.30.40/24
      # inband_mgmt_gateway: 10.20.30.1
      # inband_mgmt_vrf: INBANDMGMT

ssh_settings:
  client_vrfs:
    - name: default

expected_error_message: >-
  'ssh_settings.client_vrfs[name=default].source_interface' is required but was not found for host 'ssh-settings-client-vrfs-missing-source-interface'.
