---
type: l2leaf

l2leaf: {}

snmp_settings:
  compute_local_engineid: true
  # missing mgmt_ip to compute properly the engine ID
expected_error_message: >-
  'snmp_settings.local_engineid_ip' is set to 'use_mgmt_interface' but this node is missing 'mgmt_ip' or 'ipv6_mgmt_ip'.
