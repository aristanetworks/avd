---
fabric_name: FABRIC_FAILURE_DEVICES_AND_NODE_KEY_2

type: l2leaf

# Testing just assigning the profile is enough to trigger this.
device_profile: test

device_profiles:
  - name: test

l2leaf:
  nodes:
    - name: failure-devices-and-node-key-2

expected_error_message: >-
  Found the device 'failure-devices-and-node-key-2' under both 'l2leaf' and 'devices'.
  Remove the device from one of the models.
