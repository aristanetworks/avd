---
# Test for deprecated key vlan_inner
ip_access_lists:
  - name: ACL_SEQUENCE_AND_COUNTERS
    entries:
      - action: permit
        vlan_number: 235
        vlan_mask: "0x1FF"
        vlan_inner: true
        protocol: ip
        source: any
        destination: any
