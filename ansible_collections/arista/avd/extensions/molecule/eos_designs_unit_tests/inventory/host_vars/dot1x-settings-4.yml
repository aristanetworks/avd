---
# Test for global 802.1X enabled with combination of specific group and the default 'radius' group as fallback
type: l2leaf
l2leaf:
  defaults:
  nodes:
    - name: dot1x-settings-4
      mgmt_ip: 192.168.0.2/24

dot1x_settings:
  enabled: true
  authentication:
    radius_groups: [agni-server-group, radius]
  accounting:
    radius_groups: [agni-server-group, radius]

avd_design_future:
  # Test for new behaviour of IP RADIUS server commands
  ip_radius_source_interface_setting: true

aaa_settings:
  radius:
    servers:
      - host: agni.arista.com
        groups:
          - agni-server-group
        cleartext_key: super-secret-key
