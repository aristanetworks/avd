# Minimum config to only test the specific feature.
mgmt_interface_settings:
  vrf: MGMT
mgmt_ipv4:
  gateway: 1.1.1.1
l2leaf:
  nodes:
    - name: mgmt-interface-default
      mgmt_ip: 1.1.1.2
      id: 102
      filter:
        deny_vrfs: [MULTI_TENANT_VRF_TEST]
