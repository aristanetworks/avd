---
# Testing errdisable_settings with only detect

type: l2leaf
l2leaf:
  defaults:
  nodes:
    - name: errdisable-settings-1
      id: 42

errdisable_settings:
  detect:
    causes:
      - acl
      - arp-inspection
      - dot1x
      - link-change
      - storm-control
