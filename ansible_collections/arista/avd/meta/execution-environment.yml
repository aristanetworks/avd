# execution-environment.yml
# Read by ansible-builder when this collection is a dependency for an execution-environment
---
version: 1

dependencies:
  galaxy: requirements.yml
  # TODO: Need to see if we can replace this...
  python: requirements.txt
  system: bindep.txt
