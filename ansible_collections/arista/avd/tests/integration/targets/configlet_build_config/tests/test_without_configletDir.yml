- name: without configlet_dir
  ignore_errors: yes
  configlet_build_config:
    configlet_prefix: 'New'
    configlet_extension: 'cfg'
  register: result
- assert:
    that:
      - result is failed
      - result.changed == false
      - "'missing required arguments:' in result.msg"
