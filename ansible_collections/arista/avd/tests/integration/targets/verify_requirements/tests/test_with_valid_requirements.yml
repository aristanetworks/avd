---
- name: Test with valid Python requirements
  register: result
  arista.avd.verify_requirements:
    requirements: []
  check_mode: false
  run_once: true

- name: Validate result
  ansible.builtin.assert:
    that:
      - result is success
      - result.failed == false
