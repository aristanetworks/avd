---

type: l2leaf

l2leaf:
  nodes:
    - name: digital-twin-act-missing-os-version
      platform: vEOS-lab
      mgmt_ip: 10.0.0.1/24

platform_settings:
  - platforms: [vEOS-lab]
    digital_twin:
      platform: vEOS-lab
      act_node_type: veos

digital_twin_mode: true

expected_error_message: >-
  Failed to generate ACT Digital Twin metadata for device 'digital-twin-act-missing-os-version'.
  'os_version' attribute must be set using either the global 'digital_twin.fabric.os_version' key or the node configuration 'digital_twin.os_version' key.
