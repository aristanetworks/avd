---
# Minimum config to only test the specific feature.
type: l2leaf

l2leaf:
  nodes:
    - name: duplicate-vlans-svi-id
      id: 1

tenants:
  - name: TenantA
    vrfs:
      - name: Tenant_A_OP_Zone
        svis:
          - id: 2
            name: vlan1
  - name: TenantB
    vrfs:
      - name: Tenant_B_OP_Zone
        svis:
          # duplicate vlan_id
          - id: 2
            name: vlan2


expected_error_message: "Duplicate VLAN ID '2' found in Tenant 'TenantB' during configuration of SVI in VRF 'Tenant_B_OP_Zone'. Other VLAN is in Tenant 'TenantA'."
