---
type: l3leaf

l3leaf:
  defaults:
    loopback_ipv4_pool: 10.10.10.0/24
    vtep_loopback_ipv4_pool: 10.10.10.0/24
    bgp_as: 65042
  nodes:
    - name: management-eapi-vrf-name-use-inband-mgmt-vrf
      loopback_ipv4_address: 10.42.42.42
      id: 12

management_eapi:
  enabled: true
  vrfs:
    - name: use_inband_mgmt_vrf
      enabled: true

expected_error_message: >-
  'self.inputs.management_eapi.vrfs[name=use_inband_mgmt_vrf]' is set to 'use_inband_mgmt_vrf' but this node is missing configuration for inband management.
