---
wan_mode: sdwan
type: edge

bgp_as: 65000

edge:
  defaults:
    loopback_ipv4_pool: 192.168.0.0/24
  nodes:
    - name: invalid-wan-role-sdwan-role-1
      # Trying to set edge as RR
      wan_role: server
      id: 1

expected_error_message: >-
  'wan_role' must be 'client' when 'sdwan_role' is set to 'transit' or 'edge'
