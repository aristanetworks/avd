---
# Negative test to catch legacy syntax for hardware counter and make sure the
# error message makes sense to the user
hardware_counters:
  features:
    - qos: in

expected_error_message: "hardware_counters.features.name was expected but not set!"
