tcam_profile:
  system: traffic_policy
  profiles:
    - name: traffic_policy
      config: "{{ lookup('file', '{{ root_dir }}/inventory/TCAM_TRAFFIC_POLICY.conf') }}"
    - name: MT_TCAM_PROFILE
      source: "flash:/TCAMP_PROFILES/MY_TCAM_PROFILE.conf"
