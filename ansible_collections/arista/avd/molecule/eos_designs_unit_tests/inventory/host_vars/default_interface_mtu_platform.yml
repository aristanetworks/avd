type: l2leaf
l2leaf:
  defaults:
  nodes:
    - name: default_interface_mtu_platform

default_interface_mtu: 8765 # This will not be used since platform settings takes precedence.

platform_settings:
  - platforms: [ default ]
    default_interface_mtu: 1234
    structured_config:
      sfe:
        data_plane_cpu_allocation_max: 42
