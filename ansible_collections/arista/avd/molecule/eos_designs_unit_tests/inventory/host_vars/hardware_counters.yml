hardware_counters:
  features:
    - vlan-interface: in
    # in 3.8, this will trigger a warning in eos_cli_config_gen because of the
    # conversion
    - vlan-interface: out
    - flow-spec: in

type: l2leaf
l2leaf:
  defaults:
  nodes:
    hardware_counters:
      id: 42
