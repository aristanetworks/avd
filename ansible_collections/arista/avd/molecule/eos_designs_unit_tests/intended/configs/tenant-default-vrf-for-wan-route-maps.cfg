!
no enable password
no aaa root
!
agent KernelFib environment KERNELFIB_PROGRAM_ALL_ECMP=1
!
flow tracking hardware
   tracker FLOW-TRACKER
      record export on inactive timeout 70000
      record export on interval 300000
      exporter CV-TELEMETRY
         collector 127.0.0.1
         local interface Loopback0
         template interval 3600000
   no shutdown
!
service routing protocols model multi-agent
!
hostname tenant-default-vrf-for-wan-route-maps
!
router path-selection
   tcp mss ceiling ipv4 ingress
   !
   path-group INET id 101
      ipsec profile AUTOVPN
      !
      local interface Ethernet1
      !
      peer dynamic
   !
   load-balance policy LB-DEFAULT-POLICY-CONTROL-PLANE
      path-group INET priority 42
   !
   load-balance policy LB-DEFAULT-POLICY-DEFAULT
      path-group INET priority 42
   !
   policy DEFAULT-POLICY-WITH-CP
      default-match
         load-balance LB-DEFAULT-POLICY-DEFAULT
      !
      10 application-profile APP-PROFILE-CONTROL-PLANE
         load-balance LB-DEFAULT-POLICY-CONTROL-PLANE
   !
   vrf default
      path-selection-policy DEFAULT-POLICY-WITH-CP
!
spanning-tree mode none
!
vrf instance MGMT
!
management api http-commands
   protocol https
   no shutdown
   !
   vrf MGMT
      no shutdown
!
management security
   !
   ssl profile STUN-DTLS
      tls versions 1.2
      trust certificate aristaDeviceCertProvisionerDefaultRootCA.crt
      certificate STUN-DTLS.crt key STUN-DTLS.key
!
ip security
   ike policy AUTOVPN-IKE
      local-id 10.11.0.3
   !
   sa policy AUTOVPN-SA
   !
   profile AUTOVPN
      ike-policy AUTOVPN-IKE
      sa-policy AUTOVPN-SA
      connection start
      shared-key 7 ABCDEF1234567890
      dpd 10 50 clear
      mode transport
   !
   key controller
      profile AUTOVPN
!
interface Dps1
   description DPS Interface
   mtu 9194
   flow tracker hardware FLOW-TRACKER
!
interface Ethernet1
   description Comcast
   no shutdown
   no switchport
   ip address 10.1.1.1/24
!
interface Loopback0
   description ROUTER_ID
   no shutdown
   ip address 10.10.0.3/32
!
interface Loopback1
   no shutdown
   ip address 192.168.1.1/32
!
interface Vxlan1
   description tenant-default-vrf-for-wan-route-maps_VTEP
   vxlan source-interface Loopback1
   vxlan udp-port 4789
   vxlan vrf default vni 1
!
application traffic recognition
   !
   application ipv4 APP-CONTROL-PLANE
      destination prefix field-set PFX-PATHFINDERS
   !
   application-profile APP-PROFILE-CONTROL-PLANE
      application APP-CONTROL-PLANE
   !
   field-set ipv4 prefix PFX-PATHFINDERS
!
ip routing
no ip routing vrf MGMT
!
ip extcommunity-list ECL-EVPN-SOO permit soo 10.10.0.3:0
!
ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
   seq 10 permit 10.10.0.0/24 eq 32
!
route-map RM-CONN-2-BGP permit 10
   match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
   set extcommunity soo 10.10.0.3:0 additive
!
route-map RM-EVPN-SOO-IN deny 10
   match extcommunity ECL-EVPN-SOO
!
route-map RM-EVPN-SOO-IN permit 20
!
route-map RM-EVPN-SOO-OUT permit 10
   set extcommunity soo 10.10.0.3:0 additive
!
router bfd
   multihop interval 300 min-rx 300 multiplier 3
!
router bgp 65101
   router-id 10.10.0.3
   update wait-install
   no bgp default ipv4-unicast
   maximum-paths 16
   neighbor WAN-OVERLAY-PEERS peer group
   neighbor WAN-OVERLAY-PEERS remote-as 65101
   neighbor WAN-OVERLAY-PEERS update-source Loopback1
   neighbor WAN-OVERLAY-PEERS bfd
   neighbor WAN-OVERLAY-PEERS bfd interval 1000 min-rx 1000 multiplier 10
   neighbor WAN-OVERLAY-PEERS ttl maximum-hops 1
   neighbor WAN-OVERLAY-PEERS password 7 htm4AZe9mIQOO1uiMuGgYQ==
   neighbor WAN-OVERLAY-PEERS send-community
   neighbor WAN-OVERLAY-PEERS maximum-routes 0
   redistribute connected route-map RM-CONN-2-BGP
   !
   address-family evpn
      neighbor WAN-OVERLAY-PEERS activate
      neighbor WAN-OVERLAY-PEERS route-map RM-EVPN-SOO-IN in
      neighbor WAN-OVERLAY-PEERS route-map RM-EVPN-SOO-OUT out
      neighbor WAN-OVERLAY-PEERS encapsulation path-selection
   !
   address-family ipv4
      no neighbor WAN-OVERLAY-PEERS activate
   !
   address-family path-selection
      bgp additional-paths receive
      bgp additional-paths send any
      neighbor WAN-OVERLAY-PEERS activate
   !
   vrf default
      rd 10.10.0.3:789654
      route-target import evpn 789654:789654
      route-target export evpn 789654:789654
!
end
