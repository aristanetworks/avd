!
no enable password
no aaa root
!
vlan internal order ascending range 1006 1199
!
transceiver qsfp default-mode 4x10G
!
service routing protocols model multi-agent
!
hostname HARDWARE_FEATURES_CLOUDEOS
!
vrf instance MGMT
!
vrf instance VRF10
!
management api http-commands
   protocol https
   no shutdown
   !
   vrf MGMT
      no shutdown
!
interface Port-Channel8
   description SERVER_TEST_MTU_MRU_PO_MTU_NO_MRU
   no shutdown
   switchport
   l2 mtu 2001
!
interface Ethernet5
   no shutdown
   no switchport
   vrf VRF10
   ip address 10.10.40.3/24
!
interface Ethernet6
   description SERVER_TEST_INTERFACE_STORM_CONTROL_ETH13_NO_STORM_CONTROL
   no shutdown
   switchport
!
interface Ethernet7
   description SERVER_TEST_POE_ETH3_NO_POE
   no shutdown
   l2 mtu 1001
   switchport
!
interface Ethernet8
   description SERVER_TEST_MTU_MRU
   no shutdown
   channel-group 8 mode active
!
interface Loopback0
   description ROUTER_ID
   no shutdown
   ip address 10.10.10.3/32
!
interface Loopback1
   description VXLAN_TUNNEL_SOURCE
   no shutdown
   ip address 10.10.20.3/32
!
interface Vxlan1
   description HARDWARE_FEATURES_CLOUDEOS_VTEP
   vxlan source-interface Loopback1
   vxlan udp-port 4789
   vxlan vrf VRF10 vni 10
!
ip routing
no ip routing vrf MGMT
ip routing vrf VRF10
!
ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
   seq 10 permit 10.10.10.0/24 eq 32
   seq 20 permit 10.10.20.0/24 eq 32
!
route-map RM-CONN-2-BGP permit 10
   match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
!
router bfd
   multihop interval 300 min-rx 300 multiplier 3
!
router bgp 65001
   router-id 10.10.10.3
   no bgp default ipv4-unicast
   maximum-paths 4 ecmp 4
   neighbor EVPN-OVERLAY-PEERS peer group
   neighbor EVPN-OVERLAY-PEERS update-source Loopback0
   neighbor EVPN-OVERLAY-PEERS bfd
   neighbor EVPN-OVERLAY-PEERS ebgp-multihop 3
   neighbor EVPN-OVERLAY-PEERS send-community
   neighbor EVPN-OVERLAY-PEERS maximum-routes 0
   neighbor IPv4-UNDERLAY-PEERS peer group
   neighbor IPv4-UNDERLAY-PEERS send-community
   neighbor IPv4-UNDERLAY-PEERS maximum-routes 12000
   redistribute connected route-map RM-CONN-2-BGP
   !
   address-family evpn
      neighbor EVPN-OVERLAY-PEERS activate
   !
   address-family ipv4
      no neighbor EVPN-OVERLAY-PEERS activate
      neighbor IPv4-UNDERLAY-PEERS activate
   !
   vrf VRF10
      rd 10.10.10.3:10
      route-target import evpn 10:10
      route-target export evpn 10:10
      router-id 10.10.10.3
      neighbor 10.10.40.100 remote-as 65002
      redistribute connected
      !
      address-family ipv4
         neighbor 10.10.40.100 activate
!
end
