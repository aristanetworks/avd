---
management_ssh:
  # deprecated. Remove in 6.0
  access_groups:
    - name: ACL-SSH
    - name: ACL-SSH-VRF
      vrf: mgt
  # deprecated. Remove in 6.0
  ipv6_access_groups:
    - name: ACL-SSH6
    - name: ACL-SSH-VRF6
      vrf: mgt
  enable: true
  vrfs:
    - name: mgt
      enable: true
