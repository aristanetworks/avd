{# Spine peer filters#}
{% if switch.overlay_routing_protocol == "ebgp" %}
  LEAF-AS-RANGE:
    sequence_numbers:
      10:
        match: as-range {{ spine.leaf_as_range }} result accept
{% endif %}
