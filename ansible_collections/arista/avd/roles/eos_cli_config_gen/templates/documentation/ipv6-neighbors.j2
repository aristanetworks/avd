{#
 Copyright (c) 2023-2024 Arista Networks, Inc.
 Use of this source code is governed by the Apache License 2.0
 that can be found in the LICENSE file.
#}
{# doc - ipv6 static neighbors #}
{% if ipv6_neighbor is arista.avd.defined %}

## IPv6 Neighbors

{%     if ipv6_neighbor.cache is arista.avd.defined(true) %}
IPv6 neighbor cache is enabled.
{%     endif %}
{%     if ipv6_neighbor.persistent is arista.avd.defined(true) %}
Refresh delay to restore the IPv6 neighbor cache after reboot: {{ ipv6_neighbor.refresh_delay | arista.avd.default('600') }} seconds
{%     endif %}
{% endif %}
{% if ipv6_neighbor.static_entries is arista.avd.defined %}

### IPv6 Static Neighbors

#### IPv6 Static Neighbors Summary

| VRF | IPv6 address | Exit interface | Hardware Address |
| --- | ------------ | -------------- | ---------------- |
{%     for neighbor in ipv6_neighbor.static_entries %}
| {{ neighbor.vrf | arista.avd.default("-") }} | {{ neighbor.ipv6_address | arista.avd.default("-") }} | {{ neighbor.interface | arista.avd.default("-") }} | {{ neighbor.mac_address | arista.avd.default("-") }} |
{%     endfor %}

#### IPv6 Neighbor Configuration

```eos
{%     include 'eos/ipv6-neighbors.j2' %}
```
{% endif %}
