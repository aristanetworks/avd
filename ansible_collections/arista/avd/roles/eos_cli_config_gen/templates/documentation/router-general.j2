{% if router_general is arista.avd.defined %}
{%      set has = namespace() %}
{%      set has.found = false %}
{%      if router_general.vrfs is arista.avd.defined %}
{%          for vrf in router_general.vrfs %}
{%              if router_general.vrfs[vrf].leak is arista.avd.defined %}
{%                  set has.found = true %}
{%              endif %}
{%          endfor %}
{%      endif %}
{%      if has.found is arista.avd.defined(true) %}
### VRF Route leaking

| VRF | Source VRF | Route Map Policy |
|-----|------------|------------------|
{%          for vrf in router_general.vrfs %}
{%              if router_general.vrfs[vrf].leak is arista.avd.defined %}
{%                  if router_general.vrfs[vrf].leak.source_vrf is arista.avd.defined and router_general.vrfs[vrf].leak.policy is arista.avd.defined %}
| {{ vrf }} | {{ router_general.vrfs[vrf].leak.source_vrf }} | {{ router_general.vrfs[vrf].leak.policy }} |
{%                  endif %}
{%              endif %}
{%          endfor %}

{%      endif %}
### Router General configuration

```eos
{% include 'eos/router-general.j2' %}
```
{% else %}
Router general not defined
{% endif %}
