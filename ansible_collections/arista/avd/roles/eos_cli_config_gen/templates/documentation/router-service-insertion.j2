{#
 Copyright (c) 2023-2024 Arista Networks, Inc.
 Use of this source code is governed by the Apache License 2.0
 that can be found in the LICENSE file.
#}
{# doc - router service-insertion #}
{% if router_service_insertion.enabled is arista.avd.defined(true) %}

Router service-insertion is enabled.
{%     if router_service_insertion.connections is arista.avd.defined %}

## Connections
{%         for connection in router_service_insertion.connections | arista.avd.natural_sort('name') %}
{%             if connection.ethernet_interface.name is arista.avd.defined connection.ethernet_interface.next_hop is arista.avd.defined and connection.monitor_connectivity_host is arista.avd.defined %}

### Connections Through Ethernet Interface

| Name | Interface | Next Hop | Monitor Connectivity Host |
| ---- | --------- | -------- | ------------------------- |
{%                 break %}
{%             endif %}
{%         endfor %}
{%         for connection in router_service_insertion.connections | arista.avd.natural_sort('name') %}
{%             if connection.ethernet_interface.name is arista.avd.defined and connection.ethernet_interface.next_hop is arista.avd.defined and connection.monitor_connectivity_host is arista.avd.defined %}
| {{ connection.name }} | {{ connection.ethernet_interface.name }} | {{ connection.ethernet_interface.next_hop }} | {{ connection.monitor_connectivity_host }} |
{%             endif %}
{%         endfor %}
{%         for connection in router_service_insertion.connections %}
{%             if connection.tunnel_interface.primary is arista.avd.defined and connection.tunnel_interface.secondary is arista.avd.defined and connection.monitor_connectivity_host is arista.avd.defined %}

### Connections Through Tunnel Interface

| Name | Primary Interface | Secondary Interface | Monitor Connectivity Host |
| ---- | ----------------- | ------------------- | ------------------------- |
{%                 break %}
{%             endif %}
{%         endfor %}
{%         for connection in router_service_insertion.connections | arista.avd.natural_sort('name') %}
{%             if connection.ethernet_interface is not arista.avd.defined %}
{%                 if connection.tunnel_interface.primary is arista.avd.defined and connection.tunnel_interface.secondary is arista.avd.defined and connection.monitor_connectivity_host is arista.avd.defined %}
| {{ connection.name }} | {{ connection.tunnel_interface.primary }} | {{ connection.tunnel_interface.secondary }} | {{ connection.monitor_connectivity_host }} |
{%                 endif %}
{%             endif %}
{%         endfor %}
{%     endif %}

## Router Service-Insertion Configuration

```eos
{%     include 'eos/router-service-insertion.j2' %}
```
{% endif %}
