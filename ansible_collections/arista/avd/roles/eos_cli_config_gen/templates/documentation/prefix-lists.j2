{# doc - prefix lists #}
{% if prefix_lists is arista.avd.defined %}

### Prefix-lists

#### Prefix-lists Summary

{%     for prefix_list in prefix_lists | arista.avd.natural_sort('name') %}
##### {{ prefix_list.name }}

| Sequence | Action |
| -------- | ------ |
{%         for sequence in prefix_list.sequence_numbers | arista.avd.default([]) | arista.avd.natural_sort('sequence') %}
{%             if sequence.sequence is arista.avd.defined and sequence.action is arista.avd.defined %}
| {{ sequence.sequence }} | {{ sequence.action }} |
{%             endif %}
{%         endfor %}

{%     endfor %}
#### Prefix-lists Device Configuration

```eos
{%     include 'eos/prefix-lists.j2' %}
```
{% endif %}
