{% if vxlan_tunnel_interface is defined %}

## VXLAN Interface

### VXLAN Interface Summary

#### Source Interface: {{ vxlan_tunnel_interface.Vxlan1.vxlan.source_interface }}

#### UDP port: {{ vxlan_tunnel_interface.Vxlan1.vxlan.udp_port }}

{% if vxlan_tunnel_interface.Vxlan1.vxlan.virtual_router_encapsulation_mac_address is defined %}

#### EVPN MLAG Shared Router MAC : {{ vxlan_tunnel_interface.Vxlan1.vxlan.virtual_router_encapsulation_mac_address }}

{% endif %}

#### VLAN to VNI and Flood List Mappings

| VLAN | VNI | Flood List |
| ---- | --- | ---------- |
{%     for vlan in vxlan_tunnel_interface.Vxlan1.vxlan.vlans | arista.avd.natural_sort %}
| {{ vlan }} | {{ vxlan_tunnel_interface.Vxlan1.vxlan.vlans[vlan].vni }} | {{ vxlan_tunnel_interface.Vxlan1.vxlan.vlans[vlan].flood_vteps | join('<br/>') if vxlan_tunnel_interface.Vxlan1.vxlan.vlans[vlan].flood_vteps is defined else 'N/A' }}
{%     else %}
| N/A | N/A | N/A |
{%     endfor %}
{% if vxlan_tunnel_interface.Vxlan1.vxlan.vrfs is defined and vxlan_tunnel_interface.Vxlan1.vxlan.vrfs is not none %}

#### VRF to VNI Mappings

| VLAN | VNI |
| ---- | --- |
{%     for vrf in vxlan_tunnel_interface.Vxlan1.vxlan.vrfs | arista.avd.natural_sort %}
| {{ vrf }} | {{ vxlan_tunnel_interface.Vxlan1.vxlan.vrfs[vrf].vni }} |
{%     endfor %}
{% endif %}

#### Default Flood List

| Default Flood List |
| ---------- |
{% if vxlan_tunnel_interface.Vxlan1.vxlan.flood_vteps is defined %}
| {{ vxlan_tunnel_interface.Vxlan1.vxlan.flood_vteps | join('<br/>') }} |
{% else %}
| N/A |
{% endif %}

{%  if vxlan_tunnel_interface.Vxlan1.vxlan.flood_vtep_learned_data_plane is arista.avd.defined(true) %}

#### VXLAN flood-lists learning from data-plane
| State |
| ----- |
| Enabled |
{% endif %}

### VXLAN Interface Device Configuration

```eos
{% include 'eos/vxlan-interface.j2' %}
```
{% endif %}
