# Roles Defaults - main

avd_structured_config_file_format: "yml"

# Vault ID to use for encrypting temporary files.
# See role documentation for details.
avd_vault_id: null

# Enable debug mode for AVD.
# In debug mode, the AVD temporary directory defined by `avd_tmp_dir` is not cleaned up at the end of each role.
avd_debug: false
