# Roles Defaults - main

avd_structured_config_file_format: "yml"

# Setting this to null or and empty string will force eos_cli_config_gen to use hostvars as input.
avd_eos_cli_config_gen_input_dir: "{{ structured_dir }}"
