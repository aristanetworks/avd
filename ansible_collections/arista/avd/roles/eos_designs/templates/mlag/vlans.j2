vlans:
{% if switch.mlag_l3 is arista.avd.defined(true) %}
  {{ switch.mlag_peer_l3_vlan }}:
    tenant: system
    name: LEAF_PEER_L3
    trunk_groups:
      - LEAF_PEER_L3
{% endif %}
  {{ switch.mlag_peer_vlan }}:
    tenant: system
    name: MLAG_PEER
    trunk_groups:
      - MLAG
