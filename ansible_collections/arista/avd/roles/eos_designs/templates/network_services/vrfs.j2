{# tenant vrfs #}
vrfs:
{% for tenant in network_services_data.tenants %}
{%     for vrf in tenant.vrfs if vrf.name != 'default' %}
  {{ vrf.name }}:
    tenant: {{ tenant.name }}
    ip_routing: true
{%     endfor %}
{% endfor %}
