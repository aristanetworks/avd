{# Spine peer-filters #}
{% if underlay_rfc5549 is arista.avd.defined(true) %}
peer_filters:
  LEAF-AS-RANGE:
    sequence_numbers:
      10:
        match: "as-range {{ spine.leaf_as_range }} result accept"
{% endif %}
