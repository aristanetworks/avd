# yaml-language-server: $schema=../../../../plugins/plugin_utils/schema/avd_meta_schema.json
# Line above is used by RedHat's YAML Schema vscode extension
# Use Ctrl + Space to get suggestions for every field. Autocomplete will pop up after typing 2 letters.
type: dict
keys:
  fabric_sflow:
    documentation_options:
      filename: Fabric Variables
      table: Fabric-wide sFlow Settings
    type: dict
    description: Fabric-wide sFlow settings.
    keys:
      uplinks:
        description: Enable sFlow on all fabric uplinks.
        type: bool
      downlinks:
        description: Enable sFlow on all fabric downlinks.
        type: bool
      endpoints:
        description: Enable sFlow on all endpoints ports.
        type: bool
      l3_edge:
        description: Enable sFlow on all edge links.
        type: bool
      core_interfaces:
        description: Enable sFlow on all edge links.
        type: bool
