# yaml-language-server: $schema=../../../../plugins/plugin_utils/schema/avd_meta_schema.json
# Line above is used by RedHat's YAML Schema vscode extension
# Use Ctrl + Space to get suggestions for every field. Autocomplete will pop up after typing 2 letters.
type: dict
pattern_keys:
  "custom_structured_configuration_.+":
    documentation_options:
      filename: Custom Structured Configurations
    display_name: Custom Structured Configuration
    type: dict
    deprecation:
      warning: true
      new_key: structured_config_.+
      remove_in_version: 5.0.0
      url: https://avd.sh/en/devel/docs/porting-guides/4.x.x.html
