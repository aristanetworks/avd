# Copyright (c) 2023-2024 Arista Networks, Inc.
# Use of this source code is governed by the Apache License 2.0
# that can be found in the LICENSE file.
# yaml-language-server: $schema=../../../../plugins/plugin_utils/schema/avd_meta_schema.json
# Line above is used by RedHat's YAML Schema vscode extension
# Use Ctrl + Space to get suggestions for every field. Autocomplete will pop up after typing 2 letters.
type: dict
keys:
  wan_transit:
    documentation_options:
      table: wan-settings
    type: dict
    description: |-
      `wan_transit` node type was removed while AVD WAN development feature
      was in PREVIEW MODE.
      Migrate to using `wan_edge` node_type and setting `node.cv_pathfinder_transit_mode: region`
      on your existing transit nodes.
    deprecation:
      warning: true
      new_key: "node_type `wan_edge` and set `node.cv_pathfinder_transit_mode: region`"
      removed: true
      remove_in_version: "4.7.0"
