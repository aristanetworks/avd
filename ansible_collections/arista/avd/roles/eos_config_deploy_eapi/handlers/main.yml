---
  # handlers file for eos_config_deploy_eapi

- name: backup running config
  eos_config:
    backup: yes
    backup_options:
      filename: "{{ running_config_backup_filename }}"
      dir_path: "{{ running_config_backup_dir }}"
  listen: "backup config"