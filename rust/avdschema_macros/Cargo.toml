[package]
name = "avdschema_macros"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
avdschema = { path = "../avdschema", features = ["dump_load_files"] }

[lib]
proc-macro = true
